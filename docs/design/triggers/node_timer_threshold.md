# NodeTimerThreshold(f32)

**Scope**: Global

The node timer's remaining/total ratio dropped below the specified threshold. Sweeps all entities with BoundEffects.

Example: `NodeTimerThreshold(0.25)` fires when less than 25% of the node timer remains.
