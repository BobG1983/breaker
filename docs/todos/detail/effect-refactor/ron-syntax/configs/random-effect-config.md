# Name
RandomEffectConfig

# Parameters
- pool: list of (weight, effect) entries

# Description
- pool: Weighted list of effects to randomly select from. Each entry is a (f32 weight, EffectType) pair. Fires exactly one effect from the pool per activation.
