# Name
SpawnPhantom

# Parameters
`SpawnPhantomConfig`

# Description
Spawns a temporary phantom bolt with infinite piercing -- it passes through all cells without bouncing, damaging each one it touches. The phantom despawns after a configured duration. A maximum active count prevents flooding the screen with phantoms. See [SpawnPhantomConfig](../configs/spawn-phantom-config.md).
