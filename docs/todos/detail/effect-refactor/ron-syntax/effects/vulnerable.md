# Name
Vulnerable

# Parameters
`VulnerableConfig` — See [VulnerableConfig](../configs/vulnerable-config.md)

# Description
Increases the damage an entity takes from all sources. Typically stamped onto cells to make them easier to destroy. A cell with Vulnerable(VulnerableConfig(multiplier: 2.0)) takes double damage from every hit. Multiple vulnerability effects stack multiplicatively.
