# Name
Attraction

# Parameters
`AttractionConfig`

# Description
Steers the bolt toward the nearest entity of a configured type. The bolt's velocity is adjusted each tick to curve toward the target. This creates a homing behavior -- the bolt bends its path toward cells, walls, or the breaker depending on configuration. See [AttractionConfig](../configs/attraction-config.md).
