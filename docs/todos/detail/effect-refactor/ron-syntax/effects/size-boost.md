# Name
SizeBoost

# Parameters
`SizeBoostConfig` — See [SizeBoostConfig](../configs/size-boost-config.md)

# Description
Increases or decreases the entity's size. On a bolt, this changes the collision radius -- a bigger bolt is easier to aim but harder to thread through gaps. On a breaker, this changes the paddle width. Multiple size boosts stack multiplicatively.
