# Name
TetherBeam

# Parameters
`TetherBeamConfig`

# Description
Creates a damaging beam between two bolts. Any cell the beam passes through takes damage each tick. The beam can either connect to an existing bolt (chain mode) or spawn a new bolt to connect to. See [TetherBeamConfig](../configs/tether-beam-config.md).
