# Name
MirrorProtocol

# Parameters
`MirrorConfig`

# Description
Spawns a mirrored copy of the source bolt traveling at a reflected angle. The mirror bolt behaves independently after spawning. It can optionally inherit the source bolt's effect trees. See [MirrorConfig](../configs/mirror-config.md).
