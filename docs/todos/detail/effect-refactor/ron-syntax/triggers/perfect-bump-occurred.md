# Name
PerfectBumpOccurred

# Parameters
None

# Description
Fires on ALL entities when a perfect bump happens anywhere. Unlike PerfectBumped (which only fires on the bolt and breaker involved), this fires globally — every entity with effect trees evaluates it. Use for effects that should react to perfect bumps regardless of which entity they live on.
