# Name
DeathOccurred

# Parameters
`EntityKind` — what type of entity died.

# Description
Global version of death triggers. Fires on all entities when an entity of the specified kind dies anywhere. `DeathOccurred(Cell)` fires on every entity whenever any cell dies. `DeathOccurred(Any)` fires on every death of any kind.
