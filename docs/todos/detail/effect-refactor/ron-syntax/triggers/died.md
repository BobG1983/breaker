# Name
Died

# Parameters
None

# Description
Fires on the entity that just died. This is the victim's perspective — "I am dying." The entity is still alive when this fires (despawn is deferred), so effects can read the dying entity's components. Use for death-triggered effects like explosions on the entity that dies.
