# Name
DieConfig

# Syntax
```rust
struct DieConfig {}
```

# Description
No parameters. Sends the entity into the death pipeline. Empty struct for trait uniformity — fields may be added later with `#[serde(default)]`.
