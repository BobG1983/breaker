# Name
LoseLifeConfig

# Syntax
```rust
struct LoseLifeConfig {}
```

# Description
No parameters. Decrements the entity's Hp by 1. Empty struct for trait uniformity — fields may be added later with `#[serde(default)]`.
