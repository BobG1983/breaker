# Name
FlashStepConfig

# Syntax
```rust
struct FlashStepConfig {}
```

# Description
No parameters. Enables flash step dash on the breaker. Empty struct for trait uniformity — fields may be added later with `#[serde(default)]`.
