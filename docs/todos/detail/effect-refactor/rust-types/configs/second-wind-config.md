# Name
SecondWindConfig

# Syntax
```rust
struct SecondWindConfig {}
```

# Description
No parameters. Spawns an invisible one-shot bottom wall. Empty struct for trait uniformity — fields may be added later with `#[serde(default)]`.
