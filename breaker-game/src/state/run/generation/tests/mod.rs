//! Tests for `generation` data types — [`BehaviorKind`], [`CellConstraint`],
//! [`BlockSize`], struct shapes, validation helpers, [`DENY_LIST`], and RON
//! round-trips.

mod behavior_kind;
mod block_size;
mod cell_constraint;
mod deny_list;
mod ron_roundtrip;
mod struct_shapes;
mod validation;
