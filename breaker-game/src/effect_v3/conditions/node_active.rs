//! `NodeActive` condition evaluator.

use bevy::prelude::*;

/// Evaluate whether the `NodeActive` condition is currently true.
///
/// Returns true while a node is playing or paused.
pub fn is_node_active(_world: &World) -> bool {
    todo!()
}
