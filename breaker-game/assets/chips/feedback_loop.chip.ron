(
    name: "Feedback Loop",
    max_taken: 1,
    common: None,
    uncommon: None,
    rare: None,
    legendary: Some((prefix: "", effects: [When(trigger: OnPerfectBump, then: [When(trigger: OnImpact(Cell), then: [When(trigger: OnCellDestroyed, then: [Until(until: TimeExpires(3.0), then: [Do(SpeedBoost(multiplier: 1.5))])])])])])),
)
