(
    name: "Cascade",
    max_taken: 3,
    common: Some((
        prefix: "Minor",
        effects: [
            On(target: Bolt, then: [
                When(trigger: DestroyedCell, then: [
                    Do(Shockwave(base_range: 20.0, range_per_level: 5.0, stacks: 1, speed: 400.0)),
                ]),
            ]),
        ],
    )),
    uncommon: Some((
        prefix: "Spreading",
        effects: [
            On(target: Bolt, then: [
                When(trigger: DestroyedCell, then: [
                    Do(Shockwave(base_range: 30.0, range_per_level: 8.0, stacks: 1, speed: 400.0)),
                ]),
            ]),
        ],
    )),
    rare: Some((
        prefix: "Devastating",
        effects: [
            On(target: Bolt, then: [
                When(trigger: DestroyedCell, then: [
                    Do(Shockwave(base_range: 40.0, range_per_level: 12.0, stacks: 1, speed: 400.0)),
                ]),
            ]),
        ],
    )),
    legendary: None,
)
