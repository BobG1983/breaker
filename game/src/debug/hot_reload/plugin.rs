//! Hot-reload plugin stub — systems added in Phase 3c.

use bevy::prelude::*;

/// Stub plugin for hot-reload systems (Phase 3c).
pub struct HotReloadPlugin;

impl Plugin for HotReloadPlugin {
    fn build(&self, _app: &mut App) {
        // Systems will be added in Phase 3c.
    }
}
