---
name: Lint Patterns
description: Recurring cargo fmt and clippy patterns specific to this codebase
type: reference
---

## Cargo Formatting Rules
- Line wrap conditional expressions in assignments (bolt_breaker_collision.rs lines 43-44)
- Avoid multi-line Color::srgb calls - use single line after assignment operator
- Multi-line `assert!` with format args must be wrapped: condition, message, args on separate lines
- Multi-line method chains: wrap at logical points when necessary
- Long function call arguments: one argument per line when wrapping
- Import order: nested imports should be ordered (ClearRemainingCount, NodeSystems before systems)

## Clippy Patterns
- Type aliases required for complex Query filters (CellQueryFilter, BreakerQueryFilter)
- Use `.mul_add()` for floating point operations to satisfy suboptimal_flops lint
- Collapse nested `if let` with inner `if` condition into single `if let ... && ...` (collapsible_if lint)
- Keep test helper structs and functions at module level, not inside test functions (items_after_statements)
- Doc comments: wrap Rust identifiers and types in backticks (e.g., `HashMap`, `BreakerState`) for doc_markdown lint
- `private_interfaces` warning: `pub(crate)` structs in `chips/components.rs` (BoltSpeedBoost Amp-chip-component, BreakerSpeedBoost, WidthBoost, BumpForceBoost) referenced in `pub` query type aliases (breaker/queries.rs) and `pub` system functions. Fix: either make the chip components `pub`, or make the query aliases `pub(crate)`. Not an error — just a warning — but appears in bulk (9 instances) whenever chip components are `pub(crate)`. Note: BoltSpeedBoost here is the chips Amp chip component (chips/components.rs), not the deleted apply_bump_velocity system.
- `E0364` re-export visibility error: Stream 4 pub→pub(crate) visibility fixes introduced a mismatch between function visibility and mod.rs `pub use` re-exports. Pattern: function is `pub(crate)` in its own file but re-exported with `pub use` in `mod.rs`. The `pub use` in mod.rs must be downgraded to `pub(crate) use` to match. Affected in phase4b2: `bolt/systems/prepare_bolt_velocity.rs:18`, `breaker/systems/bump.rs:67+133` (grade_bump, update_bump), `breaker/systems/move_breaker.rs:19`.
- `similar_names` error (clippy::pedantic): test-local bindings `cell_a_y` and `cell_b_y` are too similar. Occurs in test code at `bolt/systems/bolt_cell_collision.rs` (was `physics/systems/bolt_cell_collision.rs` before 2026-03-24 extraction). Fix: rename one binding (e.g., `cell_below_y` instead of `cell_b_y`).
- `type_complexity` error: Query with 6-tuple data + filter tuple triggers this in `bolt/systems/reset_bolt.rs` (the bolt_query with Entity, Position2D, BoltVelocity, Option<PiercingRemaining>, Option<Piercing>) and `bolt/systems/spawn_bolt.rs` (bundled tuple). Fix: extract named type aliases to `bolt/queries.rs` following the `LostQuery` pattern already there. NOTE: `Option<PhysicsTranslation>` no longer appears — that type was deleted in 2026-03-24 extraction.
- `if_not_else` error (clippy::pedantic): `if !condition { A } else { B }` pattern. Fix: invert the condition to `if condition { B } else { A }`. Occurs in `chips/effects/piercing.rs:35` (`if !had_piercing`).
- `uninlined_format_args` error (clippy::pedantic): `assert!(..., "msg {}", var)` where `var` can be inlined as `{var}`. Fix: replace `{}` + trailing arg with `{var}`. Occurs in `chips/effects/mod.rs:179`.
- `dead_code` warning: `BoltHitBreaker.bolt` field in `bolt/messages.rs` (was `physics/messages.rs` before 2026-03-24 extraction) is `pub` inside a `pub(crate)` struct but never read. Recurring warning — field appears to be forward-declared for future audio/chips consumers. Not an error.
- `explicit_counter_loop` error: when an explicit `u32` counter variable is incremented in a `for` loop, clippy requires using `(0_u32..).zip(iter)` instead. Occurs in `run/systems/generate_node_sequence.rs`.
- `cast_possible_truncation` / `cast_precision_loss` errors: `usize as u32` and `u32 as f32` casts in `run/systems/generate_node_sequence.rs`. Fix: `u32::try_from(t)` for usize→u32; for u32→f32 precision loss, use `f32::from(boss_count)` (lossless for smaller u32 values, or use explicit cast with a comment if intentional).
- `dead_code` warning (chips/inventory.rs): `ChipInventory` methods `add_chip`, `stacks`, `is_maxed`, `mark_seen`, `has_seen`, `held_chips`, `maxed_chips`, `total_held` and `ChipEntry` fields `stacks`, `max_stacks`, `rarity` are unused — forward-declared for future chip consumption systems. Not errors, but appear in bulk when chip systems are not yet wired up.
- `dead_code` warning (cells/components.rs:93): `CellGridPosition` struct is never constructed — added in phase4-wave2-session4, forward-declared for future grid-position-aware systems. Not an error.
- `expect_used` warning (clippy::restriction): `u16::try_from(...).expect(...)` in `run/systems/generate_node_sequence.rs:52,58`. These are intentional — the counts are always small enough to fit u16 and the expect message explains the invariant. Not an error, but the workspace lint config enables `-W clippy::expect-used` so they appear as warnings every run.
- `collapsible_if` error (cells/definition.rs:63): `if let Some(rate) = self.behavior.regen_rate { if rate <= 0.0 || !rate.is_finite() { ... } }` — collapse to `if let Some(rate) = self.behavior.regen_rate && (rate <= 0.0 || !rate.is_finite()) { ... }`.
- `option_as_ref_deref` / `&Option<T>` error: `&Option<Res<T>>` parameters in `run/node/systems/spawn_cells_from_layout.rs:108-110` — change function signature to `Option<&Res<T>>` (or pass the inner value directly).
- `too_many_arguments` error: `spawn_cells_from_layout` at `run/node/systems/spawn_cells_from_layout.rs:127` has 9 args (limit 7). Fix: bundle related args into a helper struct or SystemParam, or split the function.
- `map_unwrap_or` error: `.map(|x| ...).unwrap_or_else(|| ...)` pattern — replace with `.map_or_else(|| fallback, |x| ...)`. Occurs at `run/systems/handle_node_cleared.rs:26`.
- `dead_code` warning (behaviors/registry.rs): `ArchetypeRegistry` methods `iter`, `values`, `len`, `is_empty` (and `clear` in test build) are never used — forward-declared for future archetype lookup systems. Not errors.
- `dead_code` warning (cells/resources.rs): `CellTypeRegistry` methods `values`, `iter`, `aliases`, `len`, `is_empty` (and `clear` in test build) are never used — forward-declared. Not errors.
- `unreachable_pub` warnings (cells/definition.rs, run/systems/generate_node_sequence.rs): `pub` items in internal modules should be `pub(crate)` or `pub(super)`. Recurring across new sessions when new public types are added.
- Format change (session 2026-03-19): single-line `assert!` now preferred when condition + message fit on one line (`cells/definition.rs:119`). Multi-argument imports collapsed to `use crate::run::{ ... }` block form (`generate_node_sequence.rs:4`). Test helper function signature collapsed to one line when short enough (`handle_node_cleared.rs:188`).
- `private_interfaces` warning (run/node/systems/spawn_cells_from_layout.rs:135): `CellSpawnContext<'_>` is `pub(crate)` but `spawn_cells_from_layout` is `pub`. Fix: downgrade function to `pub(crate)` or elevate struct to `pub`. Not an error — a warning. Added in phase4-wave2-session4. RESOLVED as of 2026-03-19l — no longer appears.
- `unreachable_pub` warnings (cells/messages.rs:9, cells/resources.rs:14): `CellDestroyed` and `CellDefaults` are `pub` structs in crate-internal modules. Clippy suggests `pub(crate)`. These are currently warnings (not errors). Added 2026-03-19l.
- `unused_imports` warning (run/node/systems/mod.rs:17): `spawn_cells_from_grid` re-exported as `pub(crate) use` but the only consumer (`debug/hot_reload/systems/propagate_node_layout_changes.rs`) imports it directly via `systems::spawn_cells_from_grid`, bypassing the re-export. Only appears in `dsclippy` (scenario runner build), not `dclippy`. Added 2026-03-19l.
- `dead_code` warnings (debug/resources.rs): `Overlay` enum, `Overlay::COUNT` const, `DebugOverlays` struct, and `flag_mut`/`is_active` methods are never used — forward-declared for future debug overlay system. Not errors.
- `unused_imports` warning (debug/resources.rs:3): `use bevy::prelude::*` is unused — visible in `dsclippy` (scenario runner build profile) and `dscheck`, not in `dclippy`. Appears as warning only. Added 2026-03-20.
- `must_use_candidate` error (scenario runner, `breaker-scenario-runner/src/runner/`): `-D clippy::pedantic` flags any `pub fn` returning a non-`()` value without `#[must_use]`. Known occurrences: `execution.rs:139` (`print_summary` returning `i32`, first seen 2026-03-20) and `discovery.rs:72` (`load_scenario` returning `Option<ScenarioDefinition>`, seen 2026-03-21). Fix: add `#[must_use]` attribute to the flagged function. Pattern: any new `pub fn` added to the scenario runner that returns a value will trigger this unless `#[must_use]` is added at write time.
- `unused-must-use` error (scenario runner, `breaker-scenario-runner/src/main.rs:170`): calling a `#[must_use]`-annotated function (`print_coverage_report`) without capturing its return value. Separate from `must_use_candidate` — this fires at the *call site*, not the definition. Fix: `let _ = print_coverage_report(&report);` to explicitly discard the return value, or capture it if the value is meaningful. Detected in `dsclippy` as `-D unused-must-use`. New as of 2026-03-21 (refactor/unify-behaviors branch).
- `match_same_arms` error (behaviors/evaluate.rs:41-44 — was bolt/behaviors/evaluate.rs before refactor/unify-behaviors): four match arms all return `inner` but pair different `TriggerKind` (was `OverclockTriggerKind`) with different `TriggerChain` variants. Clippy considers them "identical bodies" and requires merging into one arm with `|`-separated patterns. Fix: collapse all four arms into one multi-pattern arm. RESOLVED (merged) as of 2026-03-20 — but a new `manual_let_else` error appeared at the same location after the merge (see below).
- `use_self` warnings (chips/definition.rs:73,75,77,79): `Box<TriggerChain>` inside `TriggerChain` enum variants — replace `TriggerChain` with `Self`. nursery lint, warning only.
- `missing_const_for_fn` warning (chips/definition.rs:99): `is_leaf` can be `const fn`. nursery lint, warning only.
- `unused_imports` warnings (behaviors/mod.rs — was bolt/behaviors/mod.rs before refactor/unify-behaviors): `ArmedTriggers` and `EffectFired` (was `OverclockEffectFired`) re-exported as `pub(crate) use` but consumed internally via `super::` paths — no external caller imports them through the `mod.rs` re-exports. These are forward-declared for future external consumers (chips plugin, UI). Not errors. Persist as of 2026-03-21. Note: a prior memory entry incorrectly recorded these as RESOLVED; they were never externally wired, only internally used.
- `dead_code` warning (chips/definition.rs:87): `TriggerChain::depth` method is unused — forward-declared. Not an error.
- `match_same_arms` error (chips/definition.rs:123): `TriggerChain::depth` match has two separate arms both returning `0` — one covering `Shockwave | MultiBolt | Shield` and a second covering `LoseLife | SpawnBolt | TimePenalty { .. } | SpeedBoost { .. }` (was `BoltSpeedBoost { .. }` — renamed in refactor/unify-behaviors). Fix: merge both `=> 0` arms into a single arm with all patterns joined by `|`.
- `manual_let_else` error (behaviors/evaluate.rs:40-46 — was bolt/behaviors/evaluate.rs before refactor/unify-behaviors): after `match_same_arms` was fixed by collapsing to a multi-pattern arm, clippy flags the match-with-early-return as `manual_let_else`. The merged multi-pattern `let inner = match (trigger, chain) { P1 | P2 | P3 | P4 => inner, _ => return EvalResult::NoMatch }` must be rewritten as `let ((P1) | (P2) | (P3) | (P4)) = (trigger, chain) else { return EvalResult::NoMatch };`. RESOLVED as of 2026-03-20 — fix applied and confirmed clean in both `dclippy` and `dsclippy`.
- `type_complexity` error (behaviors/effects/shockwave.rs — was bolt/behaviors/effects/shockwave.rs before refactor/unify-behaviors): `handle_shockwave` system had an inline cell query triggering `clippy::type_complexity`. RESOLVED as of 2026-03-20 (overclock-trigger-chain session) — `ShockwaveCellQuery = (Entity, &Transform, Has<Locked>)` type alias extracted. Note: final query is smaller than the original (dropped `&mut CellHealth` and `Has<RequiredToClear>` since shockwave now writes DamageCell messages instead of mutating cells directly).
- `type_complexity` error (breaker/systems/width_boost_visual.rs:17): `width_boost_visual` system has inline `Query<(&BreakerWidth, Option<&WidthBoost>, &BreakerHeight, Option<&EntityScale>, &mut Transform), With<Breaker>>` with 5 data items. Fix: extract named type alias (e.g., `WidthBoostVisualQuery`) to `breaker/queries.rs`. New as of 2026-03-20 (entity_scale feature added EntityScale to this query). RESOLVED as of overclock-trigger-chain session (2026-03-20).
- `too_many_arguments` error (bolt/systems/spawn_additional_bolt.rs:26): `spawn_additional_bolt` has 8 args (limit 7). The `layout: Option<Res<ActiveNodeLayout>>` arg was added in entity_scale session. Fix: bundle related args — `meshes`, `materials`, and `bolt_config` are the natural candidates for a helper param struct, or `layout` plus `bolt_config` could form a `SpawnBoltContext` SystemParam. RESOLVED as of overclock-trigger-chain session (2026-03-20).
- `too_many_lines` error (bolt/systems/bolt_breaker_collision.rs — was physics/systems/ before 2026-03-24 extraction): `bolt_breaker_collision` function body was 102 lines. RESOLVED as of overclock-trigger-chain session (2026-03-20) — function now extracted into `resolve_overlap` helper.
- `too_many_arguments` error (bolt/systems/bolt_breaker_collision.rs): the new `resolve_overlap` private helper function had 14 args (limit 7). RESOLVED as of 2026-03-20 (overclock-trigger-chain session) — `bolt_breaker_collision` now passes `dclippy` with no errors.
- `missing_const_for_fn` warning (run/node/definition.rs:54): `fn default_entity_scale() -> f32` can be `const fn`. nursery lint, warning only. New as of entity_scale session 2026-03-20.
- `suboptimal_flops` warning (run/node/systems/spawn_cells_from_layout.rs:65): `cell_height + config.padding_y * scale` — replace with `config.padding_y.mul_add(scale, cell_height)`. nursery lint, warning only. New as of entity_scale session 2026-03-20.
- `field_reassign_with_default` error: test helper functions that build config structs via `Default::default()` + field assignments trigger this lint. Affects `shared/mod.rs:274-276,287-289` (PlayfieldConfig tests) and `run/node/systems/spawn_cells_from_layout.rs:951-956,961-967` (test helper functions). Fix: replace the `let mut x = Default::default(); x.field = val;` pattern with struct literal syntax: `PlayfieldConfig { height: 1080.0, zone_fraction: 0.667, ..Default::default() }`. New as of 2026-03-20 (zone_fraction field added to PlayfieldConfig). RESOLVED as of 2026-03-20 (session 6 Phase 3) — all 4 errors fixed.
- `missing_const_for_fn` warning (shared/mod.rs:41): `fn default_zone_fraction() -> f32` can be `const fn`. nursery lint, warning only. New as of 2026-03-20 (zone_fraction serde default fn added).
- `unused_variables` warning (run/node/systems/spawn_cells_from_layout.rs:111): `padding_y` from `ScaledGridDims` destructure. RESOLVED as of 2026-03-20 (session 6) — warning no longer appears in `dclippy` output.
- `dead_code` warning (run/node/systems/spawn_cells_from_layout.rs:32): `ScaledGridDims.scale` field is never read. Forward-declared for future scale-aware placement logic. Not an error.
- `unused_imports` warning (breaker/systems/width_boost_visual.rs:7,10): `BreakerHeight`, `BreakerWidth`, and `chips::components::WidthBoost` are imported directly but become unused once `WidthBoostVisualQuery` alias is extracted to `breaker/queries.rs`. Recurring pattern: when a type alias is extracted, the inline imports that fed the old inline Query become redundant. The fix (removing the three import items) should accompany any future changes that re-inline the query. New as of overclock-trigger-chain session (2026-03-20).
- `doc_markdown` error (cells/messages.rs:14): doc comment mentions `bolt_cell_collision` without backticks. Fix: wrap in backticks → `` `bolt_cell_collision` ``. New as of DamageCell refactor session (2026-03-20) — DamageCell message was introduced/refactored and its doc comment added this bare function reference.
- `too_many_arguments` error (bolt/systems/bolt_cell_collision.rs — was physics/systems/ before 2026-03-24 extraction): `bolt_cell_collision` now has 8 args (limit 7) after `wall_hit_writer: MessageWriter<BoltHitWall>` was added for the BoltHitWall feature. Fix: bundle related args — the three message writers (`hit_writer`, `damage_writer`, `wall_hit_writer`) are the natural candidates, or create a `CollisionWriters` SystemParam. New as of BoltHitWall session (2026-03-20).
- `unused_import` warning (chips/effects/overclock.rs:8): `ImpactTarget` is imported but not yet used — forward-declared for ImpactTarget discrimination in overclock evaluate.rs. Clears once overclock.rs uses ImpactTarget. New as of ImpactTarget/evaluate.rs session (2026-03-20).
- `unused_import` warning (cells/mod.rs:14): `handle_cell_hit` re-exported with `pub(crate) use` but not yet consumed by any caller. Forward-declared for the shockwave → DamageCell refactor; will clear once the system is wired. New as of shockwave/DamageCell refactor session (2026-03-20).
- `dead_code` warning (cells/messages.rs:27): `DamageCell.source_bolt` field (`pub source_bolt: Entity`) is never read. Forward-declared for future audio/chips consumers that need to know which bolt triggered the damage. Not an error.
- `dead_code` warning (bolt/messages.rs — was physics/messages.rs before 2026-03-24 extraction): `BoltHitCell.cell` field (`pub cell: Entity`) is never read. Forward-declared for future audio/chips consumers. Not an error. First observed on refactor/unify-behaviors (2026-03-21).
- Format: `shockwave.rs` tests needed re-formatting after DamageCell refactor — collapsed multi-line imports (`chips::{...}`), single-line `capture_damage` function signature, collapsed single-message `assert_eq!` to one line, collapsed two-component `spawn((...))` tuples to one line. Pattern: fmt prefers single-line when arguments fit within the line width limit.
- `cast_precision_loss` errors (chips/definition.rs:713,725,737,749,785,797): new Phase D stacking tests use `(stacks.saturating_sub(1)) as f32 * per_level` — the `u32 as f32` cast triggers the lint. Fix: replace `(stacks.saturating_sub(1)) as f32` with `f32::from(stacks.saturating_sub(1))` (lossless for small u32). All 6 occurrences are in test functions added with the TriggerChain leaf stacking fields. New as of TriggerChain stacking session (2026-03-20).
- `cast_precision_loss` error (behaviors/effects/shockwave.rs:42 — was bolt/behaviors/effects/shockwave.rs before refactor/unify-behaviors): `(*stacks).saturating_sub(1) as f32` in production code — same pattern, fix with `f32::from((*stacks).saturating_sub(1))`. New as of TriggerChain stacking session (2026-03-20).
- `collapsible_if` errors (behaviors/init.rs:113,118,123,128 AND debug/hot_reload/systems/propagate_archetype_changes.rs:109,114,119,124): nested `if let Some(chain) = ... { if !matches!(...) { ... } }` pattern, introduced in atomic-behaviors swap. RESOLVED as of refactor/unify-behaviors merge (2026-03-21) — SpeedBoost special-casing removed entirely (the renamed TriggerChain::BoltSpeedBoost → SpeedBoost handling was unified into the standard evaluation path).
- `doc_markdown` errors (behaviors/events.rs:9): doc comment in `EffectFired` mentions `shockwave`, `life_lost`, `time_penalty` without backticks. Fix: wrap each in backticks. New as of atomic-behaviors swap session (2026-03-21). RESOLVED as of refactor/unify-behaviors merge (2026-03-21).
- `unused_imports` warnings (behaviors/mod.rs:16,18,19): `ArmedTriggers`, `EvalResult`, `TriggerKind`, and `EffectFired` re-exported via `pub(crate) use` but have no external callers yet — forward-declared for future consumer wiring. Not errors. Moved to `behaviors/` module in atomic-behaviors swap session (2026-03-21). No longer appearing as of refactor/unify-behaviors merge (2026-03-21) — may have been wired or removed.
- `needless_pass_by_ref_mut` warning (behaviors/effects/speed_boost.rs:137): test helper `fn get_bolt_velocity(app: &mut App, entity: Entity)` takes `&mut App` but doesn't mutate it. Clippy suggests changing to `&App`. nursery lint, warning only. cfg-gated (test build only). New as of refactor/unify-behaviors merge (2026-03-21) — appeared after the TriggerChain::SpeedBoost (was BoltSpeedBoost) handler was added in the behaviors module.
- `doc_markdown` errors (breaker-scenario-runner/src/coverage.rs:62): doc comment contains bare `snake_case` and `PascalCase` without backticks. Fix: wrap both in backticks — `snake_case` and `PascalCase`. New as of 2026-03-21e (refactor/unify-behaviors). RESOLVED as of 2026-03-21 (no longer appears in dsclippy).
- `collapsible_if` errors (breaker-scenario-runner/src/lifecycle/mod.rs:331,363,369): three nested `if let Some(...) = outer { if let Some(...) = inner { ... } }` blocks. Fix: collapse each pair to `if let Some(...) = outer && let Some(...) = inner { ... }`. New as of 2026-03-21e (refactor/unify-behaviors). RESOLVED as of 2026-03-21 (no longer appears in dsclippy).
- `missing_const_for_fn` warnings (breaker-scenario-runner/src/lifecycle/mod.rs:298,430): `map_forced_game_state` and `map_scenario_breaker_state` can be `const fn`. nursery lint, warnings only. Still present as of 2026-03-21.
- `too_many_arguments` error (breaker-scenario-runner/src/lifecycle/mod.rs:445): RESOLVED as of 2026-03-21 — `PauseControl` SystemParam struct was introduced and the import was fixed. Now appears only as `private_interfaces` warning (see below).
- `SystemParam` import pattern: `SystemParam` derive is NOT in `bevy::prelude`. Every file using `#[derive(SystemParam)]` must import it explicitly: `use bevy::{ecs::system::SystemParam, prelude::*};`. Missing this import produces "cannot find derive macro `SystemParam` in this scope" at the `#[derive]` line, then cascades E0277 errors at every `.add_systems(...)` call-site in tests. This is a codebase-wide pattern — see `debug/hot_reload/systems/`, `run/node/systems/spawn_cells_from_layout.rs`, `screen/run_setup/systems/handle_run_setup_input.rs`.
- `private_interfaces` warning (breaker-scenario-runner/src/lifecycle/mod.rs:456): `PauseControl<'_>` struct is private (`pub(self)` by default, no visibility keyword) but `apply_debug_frame_mutations` is `pub`. Fix: either add `pub(crate)` to `PauseControl`, or downgrade `apply_debug_frame_mutations` to `pub(crate)`. Warning only. First seen 2026-03-21 after too_many_arguments was fixed by introducing PauseControl. RESOLVED as of 2026-03-21 (refactor/unify-behaviors) — no longer appears in dsclippy.
- `redundant_closure_for_method_calls` error (chips/offering.rs:448): `.map(|s| s.as_str())` in test — clippy requires `.map(String::as_str)` instead. Pedantic lint, error. Fix: replace the closure with the method reference directly. First seen 2026-03-22.
- `redundant_clone` warnings (chips/offering.rs:200,201,379): `.clone()` calls in test where the value is moved immediately after (no further use). nursery lint, warnings only. Fix: remove `.clone()`. First seen 2026-03-22.
- `unused_import` warning (screen/chip_select/mod.rs:10): `ChipOffers` re-exported in `pub(crate) use` but never consumed externally. Forward-declared for future chip select UI wiring. Not an error. First seen 2026-03-22.
- `seen_decay_factor` dead_code warning (chips/offering.rs:22): `OfferingConfig.seen_decay_factor` field never read — forward-declared. Not an error. First seen 2026-03-22 (dsclippy only, not in dclippy).
- `doc_markdown` errors (screen/chip_select/systems/generate_chip_offerings.rs:65): doc comment in test helper `make_registry` contains bare `Chip_0` and `Chip_1` (snake_case-like identifiers). Fix: wrap both in backticks → ``"`Chip_0`", "`Chip_1`"``. Two separate errors for two occurrences on the same line. New as of 2026-03-22.
- `match_same_arms` error (fx/transition.rs:200-201): `overlay_color` private fn has two match arms both returning `1.0` — `(Flash, In)` and `(Sweep, _)`. Fix: merge into one arm: `(TransitionStyle::Flash, TransitionDirection::In) | (TransitionStyle::Sweep, _) => 1.0`. New as of 2026-03-22. Affects both dclippy and dsclippy.
- `too_many_arguments` error (screen/chip_select/systems/handle_chip_input.rs:22): `handle_chip_input` has 8 args (limit 7): `keys`, `config`, `offers`, `selection`, `next_state`, `writer`, `inventory`, `chip_config`. Fix: bundle related args into a SystemParam — `keys + config` or `writer + selection + next_state` are natural candidates. New as of 2026-03-22. Affects both dclippy and dsclippy. RESOLVED as of 2026-03-22b — no longer appears in either crate.
- `match_same_arms` error (fx/transition.rs:200-201): RESOLVED as of 2026-03-22b — no longer appears.
- `doc_markdown` error (screen/chip_select/systems/generate_chip_offerings.rs:65): RESOLVED as of 2026-03-22b — no longer appears.
- `redundant_closure_for_method_calls` error (chips/offering.rs:448): RESOLVED as of 2026-03-22b — no longer appears.
- `missing_const_for_fn` warnings (screen/chip_select/resources.rs:59-88): ten serde `default_*` functions for `ChipSelectConfig` fields — all nursery warnings. New as of 2026-03-22b. Not errors.
- `missing_const_for_fn` warning (fx/transition.rs:189): `overlay_color` private fn can be `const fn`. nursery lint, warning only. New as of 2026-03-22b.
- `unused_imports` warning (fx/mod.rs:10): `TransitionConfig`, `TransitionDefaults`, and `TransitionOverlay` re-exported with `pub(crate) use` but not yet consumed by any external caller. Forward-declared for future transition system wiring. Not an error. New as of 2026-03-22b.
- `must_use_candidate` error (breaker-scenario-runner/src/lifecycle/mod.rs:517): `pub fn entered_playing(stats: Option<Res<ScenarioStats>>) -> bool` — new `pub fn` returning a value without `#[must_use]`. Fix: add `#[must_use]` attribute above the function signature. First seen 2026-03-22 (feature/wave-3-offerings-transitions). Pedantic lint, error in scenario runner. RESOLVED as of 2026-03-22.

## rantzsoft_physics2d Crate Patterns (first observed 2026-03-23)
- `manual_midpoint` errors (quadtree.rs:47-50, 8 occurrences): `(min.x + mid.x) / 2.0` pattern in `child_bounds` function — all 4 quadrant center expressions use the same midpoint formula twice (x and y). Fix: replace `(a + b) / 2.0` with `a.midpoint(b)` (f32 method). 8 errors total — 4 expressions, 2 coordinates each. Error under `-D clippy::pedantic`. Affects production code. RESOLVED as of 2026-03-23 — no longer appears in physics2dclippy.
- `drain_collect` error (quadtree.rs:94): `items.drain(..).collect()` — replace with `std::mem::take(items)`. Error under `-D clippy::all`. Affects production code. RESOLVED as of 2026-03-23 — no longer appears.
- `cast_precision_loss` errors (quadtree.rs:368,464,496×2,522): in test loops using `(0_u32..).zip(...)`, `f32::from(i)` where `i: u32` is NOT valid — `f32: From<u32>` is not implemented (only up to `u16`). The prior memory entry incorrectly said `i` was `usize` with `i as f32`. The actual broken state (as of 2026-03-23): these tests already tried using `f32::from(i)` but `i` is `u32` — compile errors result. Fix: change `(0_u32..).zip(...)` to `(0_u16..).zip(...)` so `i: u16`, then `f32::from(i)` is valid (`f32: From<u16>` is implemented). All 5 errors are in test-only `#[cfg(test)]` functions. Error under `-D clippy::pedantic`. RESOLVED as of 2026-03-23 — no longer appears.
- `missing_const_for_fn` warnings (aabb.rs:17, quadtree.rs:216,281,287): four constructor/accessor functions can be `const fn`. Nursery lint, warnings only.
- `use_self` warning (quadtree.rs:13): `QuadNode` used explicitly inside `impl QuadNode` — clippy suggests `Self`. Nursery lint, warning only.
- `too_many_arguments` error (quadtree.rs:80): `insert_into_node` private fn has 8 args (limit 7): `node`, `node_bounds`, `entity`, `bounds`, `layers`, `max_items_per_leaf`, `max_depth`, `depth`. Active as of 2026-03-23 (Wave 1 changes). Fix: bundle related args into a helper struct — `max_items_per_leaf`, `max_depth`, and `depth` form a natural `InsertConfig` or `QuadtreeConfig` grouping.
- `trivially_copy_pass_by_ref` error (quadtree.rs:207): `query_layers: &CollisionLayers` in `query_aabb_filtered_node` — `CollisionLayers` is 8 bytes (Copy), so passing by reference is less efficient than by value. Fix: change parameter to `query_layers: CollisionLayers`. Active as of 2026-03-23 (Wave 1 changes).

## rantzsoft_spatial2d Crate Patterns (first observed 2026-03-23)
- `should_panic_without_expect` errors (components.rs:299,305): `#[should_panic]` on two test functions without an `expected = "..."` message. Pedantic lint, errors. Fix: add `expected = "..."` matching the panic message string (e.g., `#[should_panic(expected = "Scale2D components must be non-zero")]`). Recurring pattern for any new `#[should_panic]` test in this crate.
- `missing_panics_doc` error (components.rs:119): `Scale2D::new` panics via `assert!` but has no `# Panics` section in its doc comment. Pedantic lint, error. Fix: add `/// # Panics\n/// Panics if `x` or `y` is zero (within `f32::EPSILON`).` to the function's doc comment.
- `missing_const_for_fn` warning (components.rs:135): `Scale2D::to_vec3` can be `const fn`. Nursery lint, warning only. Fix: add `const` to the fn signature.
- `too_long_first_doc_paragraph` warning (systems/propagate_position.rs:12): first doc comment paragraph spans 3 lines. Nursery lint, warning only. Fix: shorten the first paragraph to one sentence and move detail to subsequent paragraphs.
- `type_complexity` errors (systems/propagate_position.rs:20, systems/propagate_rotation.rs:18): inline `Query<(...)>` tuples with 5+ elements trigger this. Same pattern as breaker-game crate. Fix: extract named type aliases to a `queries.rs` module (or at the top of the file).
- `option_if_let_else` warnings (systems/propagate_position.rs:37, systems/propagate_rotation.rs:33): `if let Some(x) = opt { ... } else { fallback }` pattern. Nursery lint, warnings only. Fix: replace with `.map_or(fallback, |x| ...)` — e.g., `prev_pos.map_or(pos.0, |prev| prev.0.lerp(pos.0, alpha))`.
- `doc_markdown` errors (run/resources.rs:89,91,93,120): doc comments on new 4i run stat resources mention `MassDestruction`, `PerfectStreak`, `FastClear`, `ClutchClear` without backticks. Fix: wrap each identifier in backticks. Four separate errors. New as of 2026-03-22 (4i run domain GREEN).
- `must_use_candidate` error (run/resources.rs:151): `pub fn flux_earned(&self) -> u32` lacks `#[must_use]`. Fix: add `#[must_use]` above the function. New as of 2026-03-22 (4i run domain GREEN).
- `cast_precision_loss` error (run/systems/track_node_cleared_stats.rs:67): `tracker.best_perfect_streak as f32` — `u32 as f32` may lose precision. Fix: `f32::from(tracker.best_perfect_streak)`. New as of 2026-03-22 (4i run domain GREEN).
- `cast_possible_truncation` error (run/systems/track_node_cleared_stats.rs:69): attempted fix used `.min(u32::from(u16::MAX))` then `streak as u16` — clippy still flags the `as u16` cast even though .min() guarantees safety. Clippy does not infer the semantic bound. Correct fix: `u16::try_from(streak).unwrap_or(u16::MAX)` (after .min() the unwrap_or is unreachable but satisfies the type checker), OR restructure as `u16::try_from(tracker.best_perfect_streak.min(u32::from(u16::MAX))).unwrap_or(u16::MAX)` then `f32::from(that_u16)`. RESOLVED as of 2026-03-22.
- `cast_possible_truncation` + `cast_sign_loss` errors (run/systems/track_cells_destroyed.rs:93): `(2.5 / timestep.as_secs_f32()).round() as u32` — f32 to u32 cast may truncate and lose sign. Fix: use `u32::try_from(...)` with explicit rounding, or use saturating cast. New as of 2026-03-22 (4i run domain GREEN). Both dclippy errors on the same line.
- `unused_imports` warnings (run/systems/mod.rs:19,24,26-31): eight `pub(crate) use` re-exports for track_* and capture_run_seed systems — all forward-declared for future wiring. Not errors. New as of 2026-03-22 (4i run domain GREEN).
- `ChildBuilder` vs `ChildSpawnerCommands<'_>`: in Bevy 0.18.1 the child-spawner closure parameter type is `ChildSpawnerCommands<'_>`, NOT `ChildBuilder`. `ChildBuilder` does not exist in scope and causes E0425 compile errors. Every helper function that takes a `parent: &mut ChildBuilder` parameter must be changed to `parent: &mut ChildSpawnerCommands<'_>`. Pattern: writers referencing older Bevy API docs may still write `ChildBuilder`. First seen 2026-03-22 (4i screen domain GREEN — spawn_run_end_screen.rs:82,104,117,151,165).
- `similar_names` errors (screen/run_end/systems/spawn_run_end_screen.rs:17-18 AND run/systems/reset_run_state.rs:17,22): system function parameters `run_state`/`run_stats` (and `run_state`/`run_stats` in the reset system) are "too similar" under `-D clippy::pedantic`. Fix for spawn_run_end_screen: rename `run_stats` to `stats` (it is `Option<Res<RunStats>>`). Fix for reset_run_state: rename one parameter — e.g. `run_stats` → `stats`. Pattern: any system that has both `run_state: Res<RunState>` and `run_stats: Res<RunStats>` in the same parameter list will trigger this lint. New as of 2026-03-22 (4i run+screen GREEN).
- `too_many_arguments` error (screen/chip_select/systems/generate_chip_offerings.rs:30): `generate_chip_offerings` has 8 args (limit 7): `commands`, `registry`, `inventory`, `config`, `offering_config`, `offers`, `rng`, `evolution_registry`. Fix: bundle related args into a SystemParam — `registry + inventory + evolution_registry` or `config + offering_config` are natural candidates. New as of 2026-03-22c (feature/wave-3-offerings-transitions GREEN).
- `redundant_closure_for_method_calls` errors (screen/chip_select/systems/generate_chip_offerings.rs:225,361,392): `.map(|o| o.name())` — replace with `.map(ChipOffering::name)` (or the full qualified path `crate::screen::chip_select::resources::ChipOffering::name`). Three separate errors. New as of 2026-03-22c.
- `items_after_statements` errors (screen/chip_select/systems/generate_chip_offerings.rs:314-316): `use crate::chips::{...}` import block placed after earlier statements in a test function body. Fix: move the `use` block to the top of the function (before any statements). Six separate errors emitted from the one import block (one for the `use`, then one per item). New as of 2026-03-22c.
- `match_wildcard_for_single_variants` error (screen/chip_select/systems/generate_chip_offerings.rs:370): `_ => None` wildcard arm in a match on `ChipOffering` — only one variant is unhandled. Fix: replace `_ => None` with `ChipOffering::Normal(_) => None` to make exhaustiveness explicit. New as of 2026-03-22c.
- `cast_possible_truncation` error (screen/run_end/systems/spawn_run_end_screen.rs:132): `highlight.value.round() as i64` — f32-to-i64 cast may truncate. This line was itself introduced as an attempted fix for an earlier `f32 as u32` cast. The intermediate `as i64` step does NOT satisfy clippy. Correct fix: use `highlight.value.round() as u64` then `u64::try_from` — but even simpler, clippy accepts `highlight.value.clamp(0.0, u32::MAX as f32) as u32` since the f32 is first clamped to the valid range. Alternatively: `u32::try_from(i64::from(highlight.value.round() as i32)).unwrap_or(0)` — but the cleanest solution is `highlight.value.max(0.0).min(u32::MAX as f32).round() as u32` which is a lossless-within-range pattern clippy accepts, OR cast via `u64`: `u32::try_from(highlight.value.round().max(0.0) as u64).unwrap_or(u32::MAX)`. Pattern: avoid `f32 as i64` or `f32 as i32` intermediate casts — clippy flags them the same as `f32 as u32`. New/updated as of 2026-03-22 (still active in feature/wave-3-offerings-transitions).
- `single_char_pattern` errors (screen/run_end/systems/spawn_run_end_screen.rs:303,366,387,433): `.contains("5")`, `.contains("8")`, `.contains("3")`, `.contains("0")` — single-char string literals used as patterns. Fix: replace each with a `char` literal: `.contains('5')`, `.contains('8')`, `.contains('3')`, `.contains('0')`. Four errors. New as of 2026-03-22c.
- `cast_possible_truncation` + `cast_sign_loss` errors (screen/run_end/systems/spawn_run_end_screen.rs:132): `highlight.value.clamp(0.0, f32::from(u16::MAX)).round() as u16` — even with a clamp guard, clippy still flags the `as u16` cast (it does not track clamped range). Fix: use `u16::try_from(highlight.value.clamp(0.0, f32::from(u16::MAX)).round() as u32).unwrap_or(u16::MAX)` — the inner `as u32` cast is safe because the value is clamped to `[0.0, 65535.0]`, and `u16::try_from` converts the u32 without casting. Both errors are on the same expression. Active as of 2026-03-22 (feature/wave-3-offerings-transitions dclippy GREEN phase).
- `items_after_statements` error (breaker-scenario-runner/src/invariants/checkers/check_offering_no_duplicates.rs:105): `use breaker::screen::chip_select::ChipOffering;` placed after statements in a test function body. Fix: move the `use` to the top of the function (before any statements). Same pattern as the game-crate occurrence in generate_chip_offerings.rs — applies to scenario runner test code too. Error in dsclippy only. New as of 2026-03-23 (feature/wave-3-offerings-transitions). RESOLVED as of 2026-03-23 — no longer appears.
- `cast_precision_loss` error (run/systems/detect_mass_destruction.rs:50 — PRODUCTION): `count as f32` where `count` is `usize` from `.len()`. Fix: `count as f32` → `f32::from` is not lossless for usize; use `count as f32` is not accepted; clippy suggests using explicit cast. However the correct clippy-satisfying fix is to widen via `u32::try_from(count).map_or(f32::MAX, f32::from)` OR cast through u32 first since the count is small: `f32::from(u32::try_from(count).unwrap_or(u32::MAX))`. New as of 2026-03-23 (feature/wave-3-offerings-transitions).
- `cast_precision_loss` errors (run/systems/track_node_cleared_stats.rs:103,112,124 — PRODUCTION): `tracker.consecutive_no_damage_nodes as f32`, `tracker.node_bolts_lost as f32`, `tracker.total_bumps_this_node as f32` — all `u32 as f32` casts. Fix: replace `x as f32` with `f32::from(x)` (lossless for small u32 values). New as of 2026-03-23 (feature/wave-3-offerings-transitions).
- `field_reassign_with_default` errors in test helpers (run/systems/detect_mass_destruction.rs:126,159,199,232 and run/systems/track_node_cleared_stats.rs:335,367,395,528,567,602,629,662): test functions use `let mut config = HighlightConfig::default(); config.field = val;` pattern. Fix: replace with struct literal `HighlightConfig { field: val, ..Default::default() }`. Eight+ errors across the two test files. New as of 2026-03-23.
- `cast_possible_truncation` + `cast_sign_loss` errors in test helpers (run/systems/detect_mass_destruction.rs:132,170,205,248 and run/systems/track_node_cleared_stats.rs:540,576): f64-to-integer cast chain. The original `as u32` triggered this, then `as u64` intermediate also triggers it (clippy flags ALL f64-to-integer `as` casts regardless of sign). CURRENT ACTIVE ERRORS as of 2026-03-23: `detect_mass_destruction.rs:214,260` and `track_node_cleared_stats.rs:550,589` — all `(f64).ceil() as u64` pattern. The only clippy-clean fix is to avoid `as` entirely: cast as `i64` first (negative impossible at runtime but satisfies sign-loss), then `u32::try_from`. Correct pattern: `u32::try_from((target_secs / timestep.as_secs_f64()).ceil() as i64).unwrap_or(u32::MAX)` — clippy accepts `f64 as i64` for sign-loss because i64 is signed; `cast_possible_truncation` is still flagged for f64→i64 if value is huge, so the cleanest solution is to use `.floor()` or `.ceil()` first then convert via integer: `((target_secs / timestep.as_secs_f64()).ceil() as i64).try_into().unwrap_or(u32::MAX)`. Confirmed root cause: ANY `f64 as <integer>` triggers `cast_possible_truncation` + `cast_sign_loss` for unsigned targets and `cast_possible_truncation` for signed targets.

## New as of 2026-03-24f (feature/spatial-physics-extraction — chips evolution session)
- `doc_markdown` error (chips/definition.rs:1): module-level `//!` doc comment `"Chip definition types — TriggerChain variants and content types."` has bare `TriggerChain`. Fix: change to `` `TriggerChain` ``. This is the sole blocking error in both `dclippy` and `dsclippy` for this session.
- `derive_partial_eq_without_eq` warning (chips/definition.rs:238): `EvolutionRecipe` derives `PartialEq` but not `Eq`. Nursery lint, warning only. New this session.

## New as of 2026-03-25 (feature/spatial-physics-extraction — CellDestroyed deletion + clippy fixes)
- `E0107` compile error (bolt/systems/bolt_lost.rs:24,26): `MessageWriter<'w, 's, T>` used with 2 lifetime args, but Bevy 0.18.1's `MessageWriter` takes only 1 lifetime (`'w`). The `'s` arg is stale — remove it. Fix: change `MessageWriter<'w, 's, BoltLost>` → `MessageWriter<'w, BoltLost>` and same for `RequestBoltDestroyed`. This is the sole blocking compile error in both `dclippy` and `dsclippy` for this session.
- `fmt` changes (2026-03-25): auto-formatted 6 files — `effect/bridges.rs` (collapsed 2-tuple spawns and long assignments), `effect/evaluate.rs` (expanded tuple match arms with ImpactTarget), `run/node/systems/track_node_completion.rs` (blank line after `use super::*`), `run/systems/detect_combo_and_pinball.rs` (merged two separate `use` lines into one block), `run/systems/detect_mass_destruction.rs` (expanded use block), `run/systems/track_cells_destroyed.rs` (blank line after `use super::*`).
- `unused_variable` warning (effect/effects/life_lost.rs:22): system parameter `trigger: On<LoseLifeFired>` is unused. Fix: prefix with underscore → `_trigger: On<LoseLifeFired>`. Warning only.
- `unused_imports` warnings (effect/effects/mod.rs:33-42): 8 effect handler re-exports (`handle_attraction`, `handle_bolt_size_boost`, etc.) and `effect/plugin.rs:7` (`apply_once_nodes`) are forward-declared for future wiring. Not errors.
- `rantzsoft_spatial2d` new warnings (derive_transform.rs): new `derive_transform.rs` system introduced in spatial-physics-extraction has `too_long_first_doc_paragraph` (×2), `option_if_let_else` (×3), and `suboptimal_flops` (×2) nursery warnings. Same patterns as the existing `propagate_position.rs` and `propagate_rotation.rs` warnings — nursery lints, not errors.
- `unnecessary_struct_initialization` warning (quadtree.rs:118): building `TreeConfig { max_items_per_leaf: cfg.max_items_per_leaf, ... }` where all fields copy from `cfg` — replace with just `cfg`. Nursery lint, warning only.
- `redundant_clone` warning (constraint.rs:63): `let cloned = original.clone()` where `cloned` is never read after the clone. Nursery lint, warning only.

## New as of 2026-03-25b (feature/spatial-physics-extraction — post-clippy-fix verification)
- `E0107` compile errors (run/systems/detect_combo_and_pinball.rs:14,15,16): `MessageReader<'w, CellDestroyedAt>` etc. use only 1 lifetime arg — `MessageReader` in Bevy 0.18.1 takes `'w` and `'s`. Compiler hint says to add `'w` as second arg: `MessageReader<'w, 'w, CellDestroyedAt>`. This is inside a `#[derive(SystemParam)]` struct `ComboReaders<'w>`. Fix: change all three fields to `MessageReader<'w, 'w, T>`. Three errors, same file, same pattern. Sole blocking error this session — all other errors/warnings are carry-overs from prior sessions already logged.

## New as of 2026-03-24 (feature/spatial-physics-extraction — chips inventory/evolution session)
- `collapsible_if` error (chips/inventory.rs:154): nested `if let Some(tname) = template_name { if let Some(taken) = self.template_taken.get_mut(&tname) { ... } }` — collapse to `if let Some(tname) = template_name && let Some(taken) = self.template_taken.get_mut(&tname) { ... }`. This was the sole blocking error in both `dclippy` lib and `dsclippy` lib this session; it caused dsclippy to abort before reaching scenario-runner-specific code.
- `items_after_statements` errors (screen/loading/systems/seed_chip_registry.rs:196,263,312): three test functions with `use crate::chips::definition::TriggerChain;` placed after statements. Fix: move each `use` declaration to the top of its test function body (before any let/statement lines). Three separate errors. New as of this session — test code added for evolution recipe loading.
- `doc_markdown` errors (chips/definition.rs:1885, chips/inventory.rs:919, chips/offering.rs:505): doc comments on test helpers mention bare identifiers (`ChipTemplate`, `template_name`). Fix: wrap each in backticks. Three separate errors across three files.
- `dead_code` warning (chips/definition.rs:249): `ChipEffectApplied.chip_name` field (`pub chip_name: String`) is never read — forward-declared for future chip UI consumers. Warning only.
- `dead_code` warnings (chips/definition.rs:414-424): `TriggerChain::test_piercing`, `test_damage_boost`, `test_size_boost_breaker` associated functions are never used — test helper stubs forward-declared. Warning only (cfg-gated).
- `unused_imports` warning (chips/mod.rs:12): `ChipTemplate` and `expand_template` re-exported with `pub(crate) use` but have no external callers yet. Forward-declared for future chip loading consumers. Warning only.
- `redundant_clone` warnings (chips/offering.rs:246,247,425): `.clone()` calls on values that are immediately moved without further use. Three nursery warnings. Same pattern as earlier session (2026-03-22) — values cloned into `registry.insert()`.

## New as of 2026-03-24 (feature/spatial-physics-extraction — B12d dead code cleanup session)
- `doc_markdown` errors (chips/plugin.rs:47-49, chips/systems/dispatch_chip_effects.rs:1000-1002,1050): 10 total errors, all in `#[cfg(test)]` doc comments on test functions. Bare identifiers: `handle_piercing`, `handle_damage_boost`, `EffectPlugin`, `ChipsPlugin` (×2), `ChipRegistry` (×2), `OnSelected`, `EvolutionRegistry`, `ActiveEffects`. Fix: wrap each in backticks. Pattern: test-function doc comments were added without applying backtick rules. Errors in `breaker` (lib test) only — `breaker` (lib) is clean.
- New `missing_const_for_fn` warnings (scenario runner, `breaker-scenario-runner/src/invariants/checkers/check_run_stats_monotonic.rs:17,28` and `lifecycle/mod.rs:634,645`): two new functions `apply_set_run_stat` and `apply_decrement_run_stat` (plus checker methods `from_run_stats` and `is_default`) are flagged as potentially `const fn`. Nursery warnings only. The `map_forced_game_state` (308) and `map_scenario_breaker_state` (504) warnings persist from previous session (2026-03-21).
- `rantzsoft_physics2d`: new `unnecessary_struct_initialization` warning (quadtree.rs:118): `TreeConfig { max_items_per_leaf: cfg.max_items_per_leaf, max_depth: cfg.max_depth, depth: cfg.depth }` — replace with just `cfg`. Nursery warning.
- `rantzsoft_physics2d`: `missing_const_for_fn` for `SpatialIndex::new` (resources.rs:17) — new this session. Nursery warning.

## New as of 2026-03-25c (feature/spatial-physics-extraction — combo/pinball detect session)
- `private_interfaces` error (run/systems/detect_combo_and_pinball.rs:13 → run/plugin.rs:42): `ComboReaders<'w, 's>` is a `#[derive(SystemParam)]` struct defined without a visibility keyword (private to the module) inside `detect_combo_and_pinball.rs`. The function `detect_combo_and_pinball` is `pub(crate)`, and when Bevy schedules it from `run/plugin.rs`, the scheduler references the `SystemParam` type — causing `private_interfaces` to be a hard error at the `plugin.rs` registration site. Fix: add `pub(crate)` visibility to `ComboReaders` struct definition at `breaker-game/src/run/systems/detect_combo_and_pinball.rs:13`. This is the sole blocking error in both `dclippy` and `dsclippy` for this session. Pattern: any `#[derive(SystemParam)]` struct used by a `pub(crate)` system must itself be at least `pub(crate)` — private structs will compile within their own module but fail when the scheduler references them from outside.
- `rantzsoft_spatial2d`: 4 new `too_long_first_doc_paragraph` warnings from new `derive_transform.rs` and `save_previous.rs` doc comments. `option_if_let_else` warnings remain in `derive_transform.rs:79,91,103` (same pattern as the older `propagate_position.rs`/`propagate_rotation.rs` warnings). `suboptimal_flops` warnings in `derive_transform.rs:105,106` mirror existing `propagate_scale.rs:40,41` warnings. `significant_drop_tightening` warning in `plugin.rs:265` (test code). New `missing_const_for_fn` warning in `collision_layers.rs` in the physics2d crate — `interacts_with` can be `const`. All nursery, no errors.

## New as of 2026-03-24h (feature/spatial-physics-extraction — B12c typed events refactor)
- `too_many_lines` errors (effect/typed_events.rs:297, :413): `trigger_chain_to_effect` (101 lines) and `fire_typed_event` (137 lines) both exceed the 100-line limit. Both are new functions introduced by the B12c refactor that converts `TriggerChain` variants to typed effect events. Fix for `trigger_chain_to_effect`: split into smaller dispatch functions per effect group (e.g., leaf trigger arms vs. direct effect arms). Fix for `fire_typed_event`: split the match arms into helper functions grouped by effect category. Both are `-D clippy::pedantic` errors that block compilation.
- `unnecessary_wraps` error (effect/typed_events.rs:297): `trigger_chain_to_effect` returns `Option<Effect>` but returns `None` only in a `#[cfg(not(debug_assertions))]` block for non-leaf triggers. In the normal (leaf) path it always returns `Some(...)`. Clippy flags this as `unnecessary_wraps` because the function never returns `None` in the main match arms. Fix: restructure to return `Effect` directly and handle the non-leaf error case differently (e.g., panic unconditionally in debug and return a sentinel/default in release, or make the function explicitly return a Result/Option with a named error). The `unnecessary_wraps` and `too_many_lines` errors on the same function mean both must be fixed together.
- `items_after_statements` errors (effect/typed_events.rs:940,981,1014,1059,1094,1131): six test helper structs — `CapturedShockwave`, `CapturedLoseLife`, `CapturedSpeedBoost`, `CapturedPiercing`, `CapturedSizeBoost`, `CapturedPassiveSpeed` — defined inside test functions after earlier `let` statements. Fix: move each struct definition to the top of the test function (before any `let` statements), or hoist them to module level in the test module. Same pattern as `generate_chip_offerings.rs` items_after_statements from 2026-03-22c. Six errors, all in test code.
- `panic` warning (effect/typed_events.rs:395): `panic!(...)` inside a `#[cfg(debug_assertions)]` block in production code. Clippy warns about `panic` in production code (–W clippy::panic). The intent is correct (debug-only panic for invariant checking) but the warning fires because clippy does not fully respect `#[cfg(debug_assertions)]` gating. Warning only, not an error.
- `dead_code` warnings for new typed event structs (effect/typed_events.rs): All new per-effect event structs introduced by B12c (`LoseLifeFired`, `TimePenaltyFired`, `SpawnBoltFired`, `SpeedBoostFired`, `MultiBoltFired`, `ShieldFired`, `ChainLightningFired`, `SpawnPhantomFired`, `PiercingBeamFired`, `GravityWellFired`, `SecondWindFired`, and all `*Applied` structs) have fields like `bolt`, `source_chip`, `chip_name` that are never read — forward-declared for future effect handler and UI consumers. Warning only. Also `EffectFired.source_chip`, `NodeEvalResult`, `evaluate_node`, and `EffectFired` itself are dead-code forward-declarations. The `ShockwaveFired` struct itself is used (shockwave handler wired), but other structs are not yet wired.
- `missing_const_for_fn` warnings for new stub handler fns (effect/effects/*.rs): `handle_chain_lightning`, `handle_gravity_well`, `handle_piercing_beam`, `handle_second_wind`, `handle_spawn_phantom` — all no-op stub handlers added in B12c. Clippy flags them as `missing_const_for_fn` because they have empty bodies. Nursery warnings only. Fix if desired: add `const` keyword.

## New as of 2026-03-25 (feature/spatial-physics-extraction — time_pressure_boost/random_effect/entropy_engine session)
- `too_many_lines` error (effect/plugin.rs:65): `EffectPlugin::build` is 105 lines (limit 100). The new effect variants (time_pressure_boost, random_effect, chain_lightning, gravity_well, etc.) added system registrations to the build method. Fix: split build into helper methods grouped by effect category (e.g., `register_bolt_effects`, `register_cell_effects`, `register_global_effects`) and call them from build. Error in both `dclippy` and `dsclippy`.
- `too_many_lines` error (effect/typed_events.rs:353): `trigger_chain_to_effect` grew to 111 lines (was ~101 in the prior session). The new TriggerChain variants for time_pressure_boost, chain_lightning, gravity_well, random_effect, etc. added more match arms. Fix: extract arm groups into private helper fns (e.g., `chain_to_bolt_effect`, `chain_to_cell_effect`, `chain_to_global_effect`). Error in both `dclippy` and `dsclippy`. Note: `fire_typed_event` (previously 137 lines) is no longer an error — it was resolved.
- `items_after_statements` error (effect/effects/random_effect.rs:264): test-only struct `CapturedSpawn(Vec<SpawnBoltFired>)` is defined after earlier `let` statements inside a test function. Fix: move the struct definition to the top of the test function body, before any `let` statements. Only in `dclippy` (test code, not compiled in scenario runner build).
- NOTE: `too_many_lines` and `unnecessary_wraps` errors on `effect/typed_events.rs` (B12c entry above) are RESOLVED as of 2026-03-25 — no longer appear in dclippy or dsclippy.

## New as of 2026-03-25 (feature/spatial-physics-extraction — Wave 2a CellDestroyed deletion session)
- `no_effect.used_underscore_binding` error (effect/typed_events.rs:386): underscore-prefixed binding `_effect` used in a format string inside a `panic!`. Clippy forbids using `_`-prefixed bindings in format args — they are intended for "this binding is intentionally unused" but the format arg reads it. Fix: rename `_effect` to `effect` (removing the underscore). Error in `dclippy` lib only (panic path is production code).
- `too_many_arguments` error (run/systems/detect_combo_and_pinball.rs:18): `detect_combo_and_pinball` has 8 args (limit 7): `cell_destroyed_reader`, `bolt_hit_cell_reader`, `bolt_hit_breaker_reader`, `bolt_lost_reader`, `wall_hit_reader`, `cell_damaged_reader`, `writer` — likely added as `CellDestroyed` was replaced by `CellDestroyedAt`. Fix: bundle related readers into a SystemParam (e.g., `ComboEventReaders` grouping the 5-6 `MessageReader` args). Error in both `dclippy` and `dsclippy` (dsclippy fails to compile breaker lib so scenario runner is not reached).
- `useless_vec` error (effect/definition.rs:1253): test function `effect_target_multiple_entities` uses `vec![...]` but the Vec is never passed to a function requiring heap allocation — clippy requires an array literal instead (`[...]`). Fix: change `let targets = vec![...]` to `let targets = [...]`. Error in `dclippy` lib test only.
- NOTE: `too_many_lines` errors in `effect/plugin.rs` and `effect/typed_events.rs` from the previous session are RESOLVED as of this run — no longer appear.
- NOTE: dsclippy aborts at `breaker` (lib) compilation due to the two lib errors above — scenario runner code is never reached. No new scenario-runner-specific errors this session.
- NOTE: `items_after_statements` errors in `effect/typed_events.rs` (B12c entry above) are RESOLVED as of 2026-03-25 — no longer appear.

## New as of 2026-03-25 (feature/spatial-physics-extraction — B13 Archetype → Breaker rename)
- `dead_code` warnings (effect/registry.rs): `BreakerRegistry` methods `iter`, `values`, `len`, `is_empty` (and `clear` in test build) are never used — forward-declared. Registry was renamed from `ArchetypeRegistry` to `BreakerRegistry` in B13. Warning pattern unchanged from prior sessions. No new errors introduced by the rename.
- All other warnings after B13 rename are pre-existing patterns from prior sessions. Zero errors in any crate (dclippy, spatial2dclippy, physics2dclippy, dsclippy).

## New as of 2026-03-25 (feature/spatial-physics-extraction — ramping_damage + timed_speed_burst + typed events)
- `unused_import` warnings (effect/effects/mod.rs:31-40): all ten `pub(crate) use` re-exports in `effect/effects/mod.rs` are unused — `handle_attraction`, `handle_bolt_size_boost`, `handle_bolt_speed_boost`, `handle_breaker_speed_boost`, `handle_bump_force_boost`, `handle_chain_hit`, `handle_damage_boost`, `handle_piercing`, `handle_tilt_control_boost`, `handle_width_boost`. Forward-declared for future wiring. Warnings only.
- `unused_import` warnings (effect/active.rs:38): `NodeEvalResult` imported but never used in `use super::super::evaluate::{NodeEvalResult, TriggerKind, evaluate_node}`. Warning only.
- `unused_variable` warning (effect/effects/life_lost.rs:22): `trigger: On<LoseLifeFired>` parameter not prefixed with `_`. Fix: rename to `_trigger`. Warning only.
- `dead_code` warnings (chips/definition.rs TriggerChain test helpers): `test_ramping_damage` and `test_timed_speed_burst` added as new TriggerChain test helpers — forward-declared, not yet used in any test. cfg-gated. Warnings only.
- `dead_code` warnings (effect/definition.rs Effect test helpers): `test_ramping_damage` and `test_timed_speed_burst` added as Effect test helpers — same pattern. cfg-gated. Warnings only.
- `dead_code` warnings (effect/typed_events.rs — new structs): `RampingDamageApplied.max_stacks` and `RampingDamageApplied.chip_name` both unused. `TimedSpeedBurstFired.source_chip` unused. Same pattern as other `*Applied`/`*Fired` structs — forward-declared fields. Warnings only.
- `redundant_clone` warnings (rantzsoft_physics2d/src/constraint.rs:63): `original.clone()` in test — value dropped without further use. Nursery warning. New as of 2026-03-25.
- ALL four clippy passes (dclippy, spatial2dclippy, physics2dclippy, dsclippy) report 0 errors as of this session — clean except for nursery/warning-level lints.
- `unused_imports` warnings from B12c refactor: `bolt/queries.rs:13` (`TiltControlBoost`, `WidthBoost`), `chips/effects/damage_boost.rs:8` (`ChipEffectApplied`, `TriggerChain`), `effect/active.rs:38` (`NodeEvalResult`), `effect/effects/shield.rs:12` (`TriggerChain`) — all imports that were used in the generic EffectFired/ChipEffectApplied path and are no longer needed after migration to per-effect typed events. Also `bolt/systems/spawn_chain_bolt.rs:105` (`aabb::Aabb2D`) is now unused.
- `dsclippy` blocked by game crate errors: when `dclippy` fails to compile the game lib, `dsclippy` also fails at the same point (scenario runner depends on `breaker`) and never reaches scenario-runner-specific code. This means scenario runner lint results are only valid when the game crate has zero errors.
- `dead_code` warning (chips/offering.rs:69): `draw_offerings` function is never called — forward-declared for future offering display. Warning only.
- `dead_code` warning (screen/chip_select/resources.rs:47-56): four `rarity_color_*_rgb` fields of `ChipSelectConfig` never read — forward-declared for UI wiring. Warning only.
- `private_interfaces` warning (chips/inventory.rs:110): `ChipInventory::held_chips` is `pub` but its return type includes `ChipEntry` which is `pub(crate)`. Same recurring pattern — either elevate `ChipEntry` to `pub` or downgrade `held_chips` to `pub(crate)`. Warning only.
- NOTE: dsclippy did NOT reach scenario-runner-specific code in this session — it aborted after the breaker lib error. No new scenario-runner-specific patterns observed.

## New warnings observed 2026-03-24 (feature/spatial-physics-extraction)
- `unused_imports` warning (bolt/queries.rs:13): `TiltControlBoost` and `WidthBoost` imported but unused — forward-declared for query filters not yet wired. Warning only.
- `unused_import` warning (bolt/systems/spawn_chain_bolt.rs:105): `aabb::Aabb2D` imported but unused — forward-declared. Warning only.
- `dead_code` warnings (cells/components.rs): `CellWidth::half_width` and `CellHeight::half_height` methods are never used — forward-declared. Warning only. New as of spatial-physics extraction.
- `dead_code` warnings (wall/components.rs:20,22): `WallSize.half_width` and `WallSize.half_height` fields are never read — forward-declared. Warning only. New as of spatial-physics extraction.

## New errors as of 2026-03-25 (feature/spatial-physics-extraction — current session)
- `doc_markdown` error (bolt/messages.rs:57): `OnDeath` in doc comment without backticks. Fix: `OnDeath` → `` `OnDeath` ``. Same pattern.
- `doc_markdown` error (cells/messages.rs:16): same `OnDeath` bare identifier. Fix: wrap in backticks.
- `too_many_arguments` error (bolt/systems/bolt_lost.rs:33): `bolt_lost` has 8 args — `commands`, `playfield`, `rng`, `bolt_query`, `breaker_query`, `writer`, `request_destroyed_writer`, `lost_bolts`. Fix: bundle into a SystemParam.
- `doc_lazy_continuation` error (effect/bridges.rs:62): doc list item continuation line without indentation. Fix: add blank line before the continuation, OR indent it with `///   `.
- `collapsible_if` error (effect/bridges.rs:433): nested `if let EffectNode::When { .. }` with inner `if let EffectNode::Do` — collapse to single `if let`.
- `single_match_else` error (effect/bridges.rs:441): `match child { EffectNode::Do(effect) => ..., _ => ... }` — replace with `if let EffectNode::Do(effect) = child`.
- `match_wildcard_for_single_variants` errors (effect/effects/chain_bolt.rs:17, random_effect.rs:65, shockwave.rs:76, speed_boost.rs:79, effect/definition.rs:1219,1230): `_ => ` wildcard in match over `EffectTarget` or similar enum with only one remaining variant. Fix per each: replace `_` with explicit variant pattern (e.g. `EffectTarget::Location(_) => ...`).
- `type_complexity` errors (effect/effects/damage_boost.rs:38, speed_boost.rs:62, until.rs:104, until.rs:151): inline `Query<(...)>` tuples with 5+ elements. Fix: extract named type aliases to the file or a queries.rs.
- `single_match_else` error (effect/effects/random_effect.rs:52): `match node { EffectNode::Do(effect) => ..., _ => ... }` — replace with `if let EffectNode::Do(effect) = node`.
- `match_same_arms` error (effect/evaluate.rs:79): three arms all return `vec![NodeEvalResult::NoMatch]` — merge with `|` patterns.
- `match_like_matches_macro` error (effect/evaluate.rs:89): `match (kind, trigger) { ... => true, _ => false }` — replace with `matches!(...)`.
- `too_many_lines` error (effect/typed_events.rs:338): `fire_typed_event` is 169 lines (limit 100). Fix: split into helper fns grouped by effect category.
- `map_identity` errors (effect/typed_events.rs:485, 493): `.map(|(w, node)| (w, node))` is identity — remove the `.map(...)` call entirely.
- `too_many_arguments` error (run/systems/detect_mass_destruction.rs:16): 8 args. Fix: bundle into SystemParam.
- `manual_string_new` errors (chips/definition.rs:489, 512, 772): `"".to_owned()` — replace with `String::new()`.
- `used_underscore_binding` errors (effect/bridges.rs:2401, 2402): `_req` bound then used in `format!("{_req:?}")` — rename to `req` (without underscore) since it IS used.
- `no_effect_underscore_binding` errors (effect/definition.rs:895, 896): `let _copied = t; let _also = t;` — these bindings have no side-effect. Fix: either remove them or use them meaningfully.
- `match_wildcard_for_single_variants` errors (effect/definition.rs:1219, 1230): `other =>` wildcard in test helpers — replace with explicit `other @ EffectTarget::Location(_) =>` / `other @ EffectTarget::Entity(_) =>`.
- `useless_vec` error (effect/definition.rs:1248): `vec![...]` in test — replace with array `[...]` since it's not passed to a `Vec`-requiring API.
- `cast_precision_loss` error (run/systems/detect_mass_destruction.rs:153): `i as f32` where `i: usize` in test. Fix: `i as f32` in test loop — change loop variable to `u16` and use `f32::from(i)`, or use `u32::try_from(i).map_or(f32::MAX, f32::from)`.
- `items_after_statements` errors (effect/effects/speed_boost.rs:658, 683, 810 — 9 errors total): `use crate::effect::{...}` inside test functions after statements. Fix: move each `use` block to the top of its test function, before any `let` statements.
- `items_after_statements` errors (effect/effects/until.rs:325, 377, 427, 612, 669, 930, 1030): struct definitions (`struct SendMsg(...)`) inside test functions after statements. Fix: move struct defs to before any statements (top of test fn, or hoist to module level).
- `fmt` formatted 12 files this session: `bolt/systems/bolt_lost.rs`, `cells/systems/check_lock_release.rs`, `cells/systems/handle_cell_hit.rs`, `chips/definition.rs`, `chips/offering.rs`, `chips/resources.rs`, `effect/bridges.rs`, `effect/effects/entropy_engine.rs`, `effect/effects/life_lost.rs` (and several others). Pattern: `Result<MessageWriter<T>, SystemParamValidationError>` wrapping now requires line breaks after fmt formats the outer `Result<..., ...>` form.

## New as of 2026-03-25 (feature/spatial-physics-extraction — post-README vertical-slice)
- All four clippy passes (dclippy, spatial2dclippy embedded in dclippy, physics2dclippy embedded in dclippy, dsclippy) report 0 errors. Only nursery/warning-level lints remain.
- `unused_import` warning (effect/effects/entropy_engine.rs:108): `EffectTarget` imported inside a `#[cfg(test)]` block but unused. Forward-declared. Warning only.
- `needless_collect` warning (run/node/systems/spawn_cells_from_layout.rs:1465): intermediate `.collect()` before `.chain()` — clippy suggests chaining directly without collecting. Nursery lint, warning only. Fix: use the original iterator instead of collecting and re-producing.
- `fmt` triggered 0 changes this session (clean).

## Confirmed Clean as of 2026-03-23 (wave-3 post-fix verify)
- All physics2d errors (`manual_midpoint`, `drain_collect`, test `cast_precision_loss`) resolved.
- `items_after_statements` in `check_offering_no_duplicates.rs` resolved.
- `type_complexity` error that prompted a type alias extraction was confirmed clean — no new type_complexity errors.
- `save_previous_positions` renamed to `save_previous` — no lint impact observed (naming is not a lint issue here).

## rantzsoft_spatial2d Patterns (first seen 2026-03-23)
- `should_panic_without_expect` errors: `#[should_panic]` without `expected = "..."` triggers `clippy::pedantic`. Fix: always use `#[should_panic(expected = "the panic message")]` in rantzsoft_spatial2d test functions. Both occurrences are in `components.rs`.
- `missing_panics_doc` error: `pub fn new(x: f32, y: f32) -> Self` in `Scale2D` panics via `assert!` but doc comment lacks `# Panics` section. Fix: add `/// # Panics\n/// Panics if either component is zero.` above `pub fn new`.
- `missing_const_for_fn` warning (components.rs:138): `Scale2D::to_vec3` can be `const fn`. Nursery lint, warning only. Persists as of 2026-03-23 Wave 1 changes.
- `type_complexity` errors (systems/compute_globals.rs:19, systems/derive_transform.rs:20): NEW as of 2026-03-23 Wave 1 (compute_globals and derive_transform are new systems). Both inline Query tuples with 6+ elements. Fix: extract named type aliases (e.g., `ComputeGlobalsQuery`, `DeriveTransformQuery`) to a `queries.rs` module or inline at top of each file. These BLOCK the entire workspace because spatial2d is a transitive dependency of all crates.
- `similar_names` error (systems/compute_globals.rs:67): `parent_sx` and `parent_sy` are considered too similar by `clippy::pedantic`. The bindings appear in the same destructure pattern: `let Some(&(parent_pos, parent_rot, (parent_sx, parent_sy)))`. Fix: rename one — e.g., `parent_scale_x` and `parent_scale_y`, or use positional destructuring with `(sx, sy)` and rely on context. NEW as of 2026-03-23 Wave 1. BLOCKS entire workspace.
- `too_long_first_doc_paragraph` warnings: multiple new systems in Wave 1 (compute_globals.rs:15, derive_transform.rs:1 and :15, propagate_position.rs:27, save_previous.rs:7) have first doc paragraphs spanning 3 lines. Nursery lint, warnings only. Fix: shorten each to one sentence; move detail to subsequent paragraphs.
- `option_if_let_else` warnings (derive_transform.rs:50,79; propagate_position.rs:43; propagate_rotation.rs:39): `if let Some(prev) = prev_pos { ... } else { fallback }` pattern in interpolation expressions. Nursery lint, warnings only. Fix: replace with `.map_or(fallback, |prev| ...)`.
- `suboptimal_flops` warnings (derive_transform.rs:82,83; propagate_scale.rs:40,41): `prev.x + (g.x - prev.x) * alpha` interpolation expressions. Nursery lint, warnings only. Fix: `(g.x - prev.x).mul_add(alpha, prev.x)`.
- `significant_drop_tightening` warning (plugin.rs:225): test function holds a `registry.read()` guard for the entire test body. Nursery lint, warning only. Fix: `drop(registry);` after last use.
- Spatial2d Wave 1 error summary (2026-03-23): 3 errors total — 1 `similar_names` + 2 `type_complexity`. All are in new Wave 1 source files (`compute_globals.rs`, `derive_transform.rs`). These prevent compilation of ALL downstream crates (physics2d, breaker-game, scenario runner). Fix spatial2d errors first before any other crate can be verified.
- `type_complexity` errors: `propagate_position.rs:20` and `propagate_rotation.rs:18` have inline `Query<(...)>` tuples. Fix: extract named type aliases (e.g., `PropagatePositionQuery`, `PropagateRotationQuery`) to a `queries.rs` or inline in `systems/mod.rs`.
- `option_if_let_else` errors: `if let Some(prev) = prev_pos { A } else { B }` pattern in `propagate_position.rs:37` and `propagate_rotation.rs:33`. Fix: `prev_pos.map_or(pos.0, |prev| prev.0.lerp(pos.0, alpha))` and `prev_rot.map_or(rot.0, |prev| prev.0.nlerp(rot.0, alpha))` respectively.
- `duration_suboptimal_units` errors (run/systems/detect_mass_destruction.rs:135,178,218,266 and run/systems/track_node_cleared_stats.rs:590): `Duration::from_millis(5000)` when `5000ms == 5s`, and `Duration::from_millis(16000)` when `16000ms == 16s`. Fix: replace with `Duration::from_secs(5)` and `Duration::from_secs(16)` respectively. Pedantic lint, error. Pattern: any `from_millis` value that is an exact multiple of 1000 triggers this. New as of 2026-03-23 (feature/wave-3-offerings-transitions). Affects both `detect_mass_destruction.rs` (4 occurrences) and `track_node_cleared_stats.rs` (1 occurrence).
- `unused_import` warning (run/systems/mod.rs:21): `detect_mass_destruction::detect_mass_destruction` re-exported via `pub(crate) use` but not yet consumed by any external caller. Forward-declared for future run plugin wiring. Not an error. New as of 2026-03-23.
- `doc_markdown` error (physics/systems/bolt_breaker_collision.rs:961): doc comment in a test helper mentions `Position2D` without backticks: `"Breaker position is read from Transform (not Position2D)."` — fix by wrapping in backticks: `` `Position2D` ``. Introduced by the Position2D migration (wave-3). New as of 2026-03-23. RESOLVED as of 2026-03-23 — no longer appears.
- `unused_import` warning (run/plugin.rs:6): `physics::PhysicsSystems` imported but not used. Forward-declared import, warning only. New as of 2026-03-23 (Position2D migration). RESOLVED as of 2026-03-23 — no longer appears.
- `Bundle` tuple limit error (breaker/systems/spawn_breaker.rs:38): Bevy's `Bundle` impl for tuples only goes up to 15 elements. The Position2D migration added `Spatial2D`, `InterpolateTransform2D`, `Position2D`, `PreviousPosition`, and `Scale2D` to the spawn bundle, pushing the total to 16. Error: `is not a Bundle` on `commands.spawn((16-tuple...))`. Fix: nest one sub-tuple to break it below 15, or extract a `#[derive(Bundle)]` struct (e.g., `BreakerBundle`) grouping the spatial components. First seen 2026-03-23 (Position2D migration). RESOLVED as of 2026-03-23 — no longer appears.
- `cast_precision_loss` errors (run/node/systems/spawn_cells_from_layout.rs:1377,1381 — TEST): `(0..3).map(|col| Vec2::new((col as f32)...))` in the new `spawned_cell_has_position2d_at_grid_position` test. `col` is inferred as `i32` from the range; `i32 as f32` triggers the lint. Fix: change `(0..3)` to `(0_u16..3_u16)` so `col: u16` and use `f32::from(col)` instead of `col as f32`. Two errors, same pattern, both in test code. New as of 2026-03-23 (Position2D migration).
- `unused_import` warning (breaker/queries.rs:3): `use bevy::prelude::*` is now unused after the Position2D migration replaced `Transform`-based query types with `rantzsoft_spatial2d` components. Warning only. New as of 2026-03-23.
- `missing_const_for_fn` warnings (breaker-scenario-runner/src/invariants/checkers/check_run_stats_monotonic.rs:17,28): `from_run_stats` and `is_default` private methods can be `const fn`. Nursery lint, warnings only. First seen 2026-03-23e (feature/wave-3-offerings-transitions).
- `missing_const_for_fn` warnings (breaker-scenario-runner/src/lifecycle/mod.rs:646,657): `apply_set_run_stat` and `apply_decrement_run_stat` private functions can be `const fn`. Nursery lint, warnings only. First seen 2026-03-23e.
- `too_long_first_doc_paragraph` warning (breaker-scenario-runner/src/lifecycle/mod.rs:399): doc comment on `apply_debug_frame_mutations_deferred` spans more than one sentence on the first paragraph. Nursery lint, warning only. First seen 2026-03-23e.
- `unused_import` warning (wall/systems/spawn_walls.rs:336): `Position2D` imported alongside `Scale2D` in a test-local `use` statement but only `Scale2D` is used. Fix: remove `Position2D` from the import. Warning only. New as of 2026-03-23.

## Active Errors as of 2026-03-24d (feature/spatial-physics-extraction)
- `doc_markdown` error (`behaviors/effects/multi_bolt.rs:1`): module-level `//!` doc has bare `MultiBolt` without backticks. Fix: change to `` `MultiBolt` ``. Error in both `dclippy` and `dsclippy`.
- `cast_precision_loss` error (`behaviors/effects/shield.rs:52`): `stacks.saturating_sub(1) as f32` — u32→f32 cast. Fix: `f32::from(stacks.saturating_sub(1))` is NOT valid (f32: From<u32> not implemented). Must go through u16: `f32::from(u16::try_from(stacks.saturating_sub(1)).unwrap_or(u16::MAX))` or cast through intermediate that fits. Alternatively `stacks.saturating_sub(1).min(u32::from(u16::MAX)) as f32` then wrapped. Error in both `dclippy` and `dsclippy`.
- `too_many_lines` error (`bolt/systems/bolt_breaker_collision.rs:48`): `bolt_breaker_collision` function body 101 lines (limit 100). Fix: extract a helper function to reduce the body by 1+ lines. Error in both `dclippy` and `dsclippy`.
- `empty_string_literal` error (`bolt/systems/spawn_additional_bolt.rs:509`): `"".to_owned()` — replace with `String::new()`. Test-build only (`dclippy` lib test).
- `uninlined_format_args` error (`bolt/systems/spawn_additional_bolt.rs:584,589`): `assert!(..., "msg {}", var)` where var can be inlined as `{var}`. Two occurrences. Test-build only (`dclippy` lib test).

## Previously Active — Resolved as of 2026-03-24b
- `too_many_lines` error (`bolt/systems/bolt_cell_collision.rs:76`): RESOLVED — bolt_cell_collision is now clean.
- `doc_markdown` error (`bolt/systems/bolt_cell_collision.rs:1900`): RESOLVED.

## Confirmed Clean as of 2026-03-24b (feature/spatial-physics-extraction — dclippy game crate)
- All 20 errors recorded in the 2026-03-24 session are resolved.
- `cast_precision_loss` in `select_highlights.rs`: fixed via `config_f32()` helper (converts u32→u16→f32 losslessly). NOTE: `f32::from(u32)` is NOT valid — `From<u32>` is not implemented for `f32`. Must go through u16.
- `cast_precision_loss` in `spawn_highlight_text.rs`: fixed.
- `doc_markdown` (animate_punch_scale.rs, run/definition.rs): fixed.
- `cast_possible_wrap`, `float_cmp` in select_highlights.rs: fixed.
- New warning: `unused_import: aabb::Aabb2D` at `bolt/systems/spawn_chain_bolt.rs:99` — first seen 2026-03-24b. Forward-declared import. Not an error.

## Confirmed Clean as of 2026-03-23 (feature/wave-3-offerings-transitions — post wave-3 lint run)
- All four crates (`dclippy`, `spatial2dclippy`, `physics2dclippy`, `dsclippy`) produced 0 errors.
- `cargo fmt --check` also clean (no files changed).
- New warnings introduced this session (all pre-existing patterns, no new lint categories):
  - `breaker-game` (dclippy): `unused_imports` (TiltControlBoost, WidthBoost in bolt/queries.rs; BreakerHeight, BreakerWidth, WidthBoost in width_boost_visual.rs; handle_cell_hit in cells/mod.rs; ImpactTarget in overclock.rs; TransitionConfig/Defaults/Overlay in fx/mod.rs); `private_interfaces` (AugmentEffect, ChipEntry); `dead_code` (rarity_color_* fields, registry methods, BoltHitCell.cell, CellGridPosition, DamageCell.source_bolt, TriggerChain.depth, ChipEntry.rarity, EvolutionRegistry.insert); `missing_const_for_fn` (serde defaults in chip_select/resources.rs, ChipOffering.definition, default_zone_fraction, overlay_color, default_entity_scale, RunStats.flux_earned); `derive_partial_eq_without_eq` (chips/definition.rs:175); `suboptimal_flops` (spawn_cells_from_layout.rs:68); `redundant_clone` (chips/offering.rs:197,198,376); `needless_collect` (spawn_cells_from_layout.rs:1377); `needless_pass_by_ref_mut` (speed_boost.rs:137).
  - `rantzsoft_spatial2d` (spatial2dclippy): `missing_const_for_fn` (Scale2D.to_vec3); `too_long_first_doc_paragraph` (propagate_position.rs:27); `option_if_let_else` (propagate_position.rs:43, propagate_rotation.rs:39); `suboptimal_flops` (propagate_scale.rs:40,41). All nursery warnings.
  - `rantzsoft_physics2d` (physics2dclippy): `use_self` (quadtree.rs:13) — nursery warning only.
  - `breaker-scenario-runner` (dsclippy): `missing_const_for_fn` (check_run_stats_monotonic.rs:17,28; lifecycle/mod.rs:314,517,647,658); `too_long_first_doc_paragraph` (lifecycle/mod.rs:400). All nursery warnings.

## New Patterns as of 2026-03-24 (feature/spatial-physics-extraction)
- `unnecessary_struct_initialization` warning (rantzsoft_physics2d/src/quadtree.rs:118): `TreeConfig { max_items_per_leaf: cfg.max_items_per_leaf, max_depth: cfg.max_depth, depth: cfg.depth }` copies all fields from `cfg` — clippy suggests replacing with `cfg` directly. Nursery lint, warning only. Appeared after `TreeConfig` struct was introduced for the `insert_into_node` refactor.
- `redundant_clone` warning (rantzsoft_physics2d/src/constraint.rs:63): test code `let cloned = original.clone()` — `original` is dropped without further use after the clone. Nursery lint, warning only. Fix: remove `.clone()` or use `original` directly.
- `significant_drop_tightening` warning (rantzsoft_spatial2d/src/plugin.rs:265): test function holds a `registry.read()` guard for the entire test body — clippy suggests dropping it earlier with `drop(registry);` after last use. Nursery lint, warning only (test build only, `significant_drop_tightening`).
- `unwrap_used` warning (run/systems/spawn_highlight_text.rs:93): `a.1.partial_cmp(&b.1).unwrap()` in `sort_by` closure. `partial_cmp` on `f32` returns `None` only for NaN; the unwrap is intentional but clippy's `-W clippy::unwrap-used` flags it. Fix: use `sort_by(|a, b| a.1.total_cmp(&b.1))` (avoids the Option entirely) or `partial_cmp(...).unwrap_or(Ordering::Equal)`. New as of 2026-03-24 (spatial-physics-extraction branch).
- `dead_code` warnings — new forward-declared items on feature/spatial-physics-extraction:
  - `run/systems/select_highlights.rs`: `config_f32` (private fn), `score_highlight` (pub(crate) fn), `select_highlights` (pub(crate) fn) — three functions in a not-yet-wired module. Warning only.
  - `fx/components.rs:25`: `PunchScale` struct fields (`timer`, `duration`, `overshoot`) — forward-declared for punch scale animation. Warning only.
  - `fx/systems/animate_punch_scale.rs:9`: `animate_punch_scale` function — not yet wired into the fx plugin. Warning only.
  - `wall/components.rs:20`: `WallSize` struct fields (`half_width`, `half_height`) — forward-declared. Warning only.
  - `cells/components.rs:37,48` (dsclippy only): `CellWidth.0` and `CellHeight.0` field values are never read — only appears in test-build profile. Warning only.
- New `option_if_let_else` + `suboptimal_flops` warnings in `rantzsoft_spatial2d/src/systems/derive_transform.rs` (lines 79,91,103,105,106): two new interpolation `if let Some` blocks for `prev_pos`, `prev_rot`, `prev_scale` in the new `derive_transform` system. Same pattern as `propagate_position.rs` and `propagate_rotation.rs` already in memory. All nursery warnings.
- `redundant_closure_for_method_calls` errors (run/systems/spawn_highlight_text.rs:567,750): `|a, b| a.total_cmp(b)` used in two `sort_by` calls. Fix: replace with `f32::total_cmp` method reference. Pedantic lint, errors. Appeared 2026-03-24c — the `unwrap_used` fix at spawn_highlight_text.rs:93 that introduced `total_cmp` was cleaned for the unwrap but left redundant closures at two call sites. Pattern: whenever `sort_by(|a, b| a.method(b))` can be written as `sort_by(Type::method)`, clippy requires the latter.
- `missing_const_for_fn` warnings (rantzsoft_physics2d/src/collision_layers.rs:28, rantzsoft_physics2d/src/resources.rs:17): `CollisionLayers::interacts_with` and `SpatialIndex::new` can be `const fn`. Nursery lint, warnings only. New as of 2026-03-24 (spatial-physics-extraction).
- `use_self` warning (rantzsoft_physics2d/src/quadtree.rs:13): `Box<[QuadNode; 4]>` inside `impl QuadNode` — replace `QuadNode` with `Self`. Same pattern as noted for prior physics2d crate work. Nursery lint, warning only.
- `missing_const_for_fn` warnings (breaker-scenario-runner, new functions in lifecycle/mod.rs:634,645): `apply_set_run_stat` and `apply_decrement_run_stat` are new private functions on this branch. Nursery, warnings only. (lifecycle/mod.rs line numbers shifted from prior entries — now 634 and 645 instead of 647 and 658.)
