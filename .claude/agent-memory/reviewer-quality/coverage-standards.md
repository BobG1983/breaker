---
name: Coverage Standards & Doc Conventions
description: Test coverage standards by domain and documentation conventions
type: reference
---

## Test Coverage Standards by Domain

- **spawn_cells_from_grid / CellBehavior wiring (CLOSED 2026-03-19 verification-fixes branch)**: The previously reported logic gap (locked/regen_rate never wired) is now FIXED. `spawn_cells_from_grid` now reads `def.behavior.locked` and `def.behavior.regen_rate` and inserts `Locked`, `LockAdjacents`, and `CellRegen` at spawn time. Full behavior-wiring tests added in `spawn_cells_from_layout.rs`: `locked_cell_definition_spawns_with_locked_component`, `non_locked_cell_does_not_have_locked_component`, `locked_cell_definition_spawns_with_lock_adjacents_component`, `regen_cell_definition_spawns_with_cell_regen_component`, `non_regen_cell_does_not_have_cell_regen_component`. Gap is CLOSED.

- **Bump system**: Very high. Grade functions, update_bump, grade_bump, combined pipeline, BoltServing guard, input-loss regression, perfect_bump_dash_cancel.
- **CCD physics**: Comprehensive. All collision surfaces, multi-bolt, cascade prevention, MAX_BOUNCES cap, wall vs cell, overlap resolution.
- **clamp_bolt_to_playfield (as of 2026-03-19, develop branch)**: Now has 9 tests including `corner_escape_both_axes_clamped`. Previous gap (3) CLOSED. Known remaining gaps: (1) no test for a bolt exactly at the clamp boundary (pos.x == x_max) — behavior defined but boundary untested. (2) left-wall clamp does not assert vy unchanged (only right-wall does). (3) corner escape test exists but only tests both-positive velocity case — no test for partially-inward velocity on corner escape.
- **Breaker state machine**: All 4 transitions, easing correctness, frame-rate independence, timer init.
- **Node completion**: All branches (required, non-required, zero, remaining).
- **Run end paths**: All three outcomes (node-transition, win, no-op).
- **Scenario invariants (as of 2026-03-19, develop branch)**: Very high. All checkers have happy-path, sad-path, and edge-case tests including physics_frozen_during_pause. Previous gaps are now CLOSED: `check_bolt_in_bounds` now has `bolt_in_bounds_increments_invariant_checks_in_scenario_stats` test. `Settling → Dashing` and `Braking → Settling` tests added to `check_valid_breaker_state`. No remaining known gaps in this area.
- **ScenarioVerdict (verdict.rs)**: Very high as of 2026-03-18. All 6 health checks tested (no_actions_injected, never_entered_playing, no_bolts_tagged, no_breakers_tagged, early_exit, no_invariant_checks). All 3 expected_violations branches tested (None/no-violations → Pass, Some([]) → Pass, expected matches → Pass, expected not fired → Fail, unexpected violation → Fail). Logs cause failure. add_fail_reason semantics (appends, keeps Fail, reverts Pass to Fail). Default verdict is Fail with sentinel reason. Known gap: `collect_and_evaluate` in runner.rs has no unit test for the missing-resource path (the 4-way match arm that adds individual resource-missing reasons).
- **runner.rs grouping helpers (as of 2026-03-18, feature/scenario-runner-dedup-summary)**: group_violations fully covered (same-kind grouping, different-kind separation, single entry). group_logs now covered including same-message-different-levels separation. is_invariant_fail_reason now tested exhaustively against InvariantKind::ALL and health-check strings. is_health_check_reason has no direct unit test (tested indirectly via is_invariant_fail_reason). collect_and_evaluate missing-resource path (4-way match arm) still untested. scenario_log_plugin() helper is private and not directly tested (acceptable — it's simple config construction).
- **runner.rs run_all_parallel / run_all_serial / parse_parallelism / build_run_list / Parallelism (as of 2026-03-19)**: parse_parallelism fully covered (all, ALL, positive int, zero, non-numeric). Parallelism::resolve covered (Count→value, All→total, All→at-least-one, Count(0)→1). Parallelism::Display covered. build_run_list covered (single scenario, --all, nonexistent). print_summary covered (all-pass, any-fail). replicate_run_list covered (copies=5, copies=1, copies=0). run_all_parallel: no unit tests — spawn-failure path, wait-failure path, stdout/stderr capture untested. run_all_serial: no unit tests. parse_loop_count in main.rs: covered (positive, zero, non-numeric). Known remaining gaps: (1) replicate_run_list with multi-entry input list not tested. (2) run_all_parallel/serial remain integration-only. (3) Parallelism::All with empty list (copies == 0) in copies computation path (main.rs line 62) produces copies=1 not copies=0 — this case is untested.
- **game.rs (as of 2026-03-18, feature/scenario-runner-dedup-summary)**: `game_plugin_group_builds` tests the non-headless path. `headless_game_spawns_no_camera` tests the headless path (no Camera2d spawned). `HeadlessAssetsPlugin` is private — it is implicitly exercised by `headless_game_spawns_no_camera` (the app.update() would fail asset init if the plugin were broken). No standalone `headless_assets_plugin_builds` test exists; acceptable for a private plugin covered transitively.
- **spawn signal systems (as of 2026-03-18, feature/scenario-runner-dedup-summary)**: New tests added for spawn_bolt, spawn_breaker, spawn_walls, spawn_cells_from_layout, and check_spawn_complete. All cover entity count, message emission, position, and cleanup marker. Known gaps: (1) check_spawn_complete does not test each individual missing signal — only tests 2-of-4 missing. (2) spawn_breaker `no_double_spawn` test checks entity count but not that BreakerSpawned is still sent when a breaker already exists (that path is critical for the coordinator). (3) check_no_entity_leaks (EntityLeakBaseline) has no test for baseline reset on a second SpawnNodeComplete (node transition). (4) message debug format tests in messages.rs files are low-value (derive coverage) but not a gap.
- **render_assets tuple pattern**: `(ResMut<Assets<Mesh>>, ResMut<Assets<ColorMaterial>>)` used in spawn_bolt and spawn_cells_from_layout — intentional Bevy workaround for multiple ResMut borrows. Do not flag.
- **bolt/queries.rs**: Module doc misleading (says "clippy type_complexity lint" when real reason is project convention — complex query aliases in their own file). Flagged in 2026-03-18 and confirmed still unresolved as of 2026-03-19 review. Fix: change the `//!` doc to say "Query types with 5+ components live here rather than inline in system files."
- **cells/queries.rs**: CLOSED 2026-03-19 (phase4b2 review) — the module doc was already corrected to say "Query types with 4+ components live here rather than inline in system files."
- **physics/queries.rs**: Module doc FIXED in feature/phase4b2-effect-consumption — now correctly says "complex query tuples live here rather than inline in system files." CLOSED.
- **update_timer_display.rs**: `total == 0.0` divide-by-zero path — CLOSED in 2026-03-19 Local<Vec> perf fix PR (`total_zero_does_not_divide_by_zero` test added).
- **valid_breaker_state.rs (Settling→Dashing)**: Legal `Settling → Dashing` transition untested — CLOSED in 2026-03-19 PR (`valid_breaker_state_does_not_fire_on_settling_to_dashing` test added). Braking→Settling also added in same PR.
- **runner/app.rs `collect_and_evaluate` empty-buffer path**: CLOSED in 2026-03-19 PR (`collect_and_evaluate_fails_when_no_snapshot` test added). Remaining gap: happy-path where a snapshot IS present and passes/fails is still not unit-tested (tested only via full integration runs).
- **read_input.rs**: `repeat: true` key event filter path untested. The `if !event.state.is_pressed() || event.repeat { continue; }` guard skips repeated key events, but no test exercises this path.
- **bolt_cell_collision.rs module doc**: Partially updated in phase4b2. Now correctly describes MAX_BOUNCES and natural cascade prevention. Still missing: mention of piercing path where multiple cells CAN be hit per frame budget without reflecting. Flagged 2026-03-19 phase4b2 review.
- **runner/execution.rs stress infrastructure (as of 2026-03-19, feature/ron-stress-testing)**: `StressResult::passed`, `summary_line`, and `pass_count` fully covered. `partition_stress_scenarios` covered for normal+stress mix, all-normal, empty input, and returns-default-config. `run_single_scenario` is new (added this branch) — no direct unit test exists for it. `SubprocessSpec`/`ChildResult` are private structs; tested only transitively through `run_all_parallel`/`run_stress_scenario`. Known gaps: (1) `run_stress_scenario` exe-lookup-error early-return path untested. (2) `print_stress_result` verbose stderr branch untested (comment in doc says "not yet implemented"). (3) `run_single_scenario` has no unit test — only exercised via integration run through `main`.
- **types/mod.rs StressConfig (as of 2026-03-19)**: Full RON parse, empty-struct defaults, and two partial-override variants tested. ScenarioDefinition stress field (None/Some/Some(()) defaults) all tested.

## CCD / Piercing (bolt_cell_collision.rs, as of 2026-03-19, feature/phase4b2-effect-consumption)
Coverage: comprehensive for normal reflection, multi-bolt, cascade prevention, MAX_BOUNCES, wall reset. New piercing tests cover: non-piercing reflects, piercing passes through destroyable cell, reflects off survivable cell, DamageBoost lookahead for piercing decision, two stacked cells pierced in one frame, per-bolt pierce skip set, wall hit resets PiercingRemaining.
Known gaps: (1) No test for `PiercingRemaining(0)` with a cell the bolt would destroy — bolt should reflect (exhausted pierces path). (2) No test for bolt that has `PiercingRemaining` but NOT `Piercing` — wall-hit guard `if let (Some(pr), Some(p))` correctly skips but this is untested. (3) Two truly adjacent cells (gap == cell_height + padding) not explicitly tested for multi-pierce in one frame — the existing test uses non-adjacent cells.

## Chips domain — B12c typed events (apply_chip_effect.rs + effects/ + effect/effects/, as of 2026-03-24, feature/spatial-physics-extraction)
- `typed_events.rs` struct-construction tests: all 21 event types have a construction test confirming field access. Comprehensive for shape correctness but NONE of the stub event tests (`stub_event_chain_lightning_fired_accessible`, `stub_event_spawn_phantom_fired_accessible`, `stub_event_piercing_beam_fired_accessible`, `stub_event_gravity_well_fired_accessible`, `stub_event_second_wind_fired_accessible`) test dispatch behavior — acceptable for stub-phase handlers.
- `typed_events.rs` `fire_typed_event` dispatch: Only Shockwave and LoseLife are covered by the `fire_typed_event_dispatches_*` tests visible in the file. Missing dispatch tests for: TimePenalty, SpawnBolt, SpeedBoost, ChainBolt, MultiBolt, Shield, ChainLightning, SpawnPhantom, PiercingBeam, GravityWell, SecondWind. Gap is acceptable during stub phase; flag for migration completion.
- `typed_events.rs` `fire_passive_event` dispatch: No `fire_passive_event_dispatches_*` tests exist — the function is tested only indirectly through the handler-level observer tests. Acceptable given observer tests cover the full round-trip.
- `damage_boost.rs` tests: still 2 new typed-event tests (insert + stack). Missing: `damage_boost_applied_respects_max_stacks` and `ignores_non_matching_effects` (typed variant). Gap (4) from prior tracking remains OPEN.
- `chain_hit.rs` tests: 3 tests only (insert + stack + cap-via-old-event). Missing: `ignores_non_matching_effects` (typed variant). Gap remains OPEN.
- `bump_force_boost.rs` tests: 3 tests only (insert + stack + cap-via-old-event). Missing: `ignores_non_matching_effects`. Gap remains OPEN.
- `tilt_control_boost.rs` tests: 3 tests only (insert + stack + cap-via-old-event). Missing: `ignores_non_matching_effects`. Gap remains OPEN.
- `chain_lightning.rs` handler is a no-op stub — test only checks that LoseLife (wrong event) does not panic. No test for the typed `ChainLightningFired` event being received (handler is wired). Acceptable for stub phase.
- `convert_target` function in `typed_events.rs`: no unit tests. Function is 3-line enum mapping; tested indirectly through `trigger_chain_to_effect` which is tested by `fire_typed_event_dispatches_shockwave` etc. Acceptable.
- `apply_chip_effect.rs` dispatch logic: well covered for OnSelected multi-leaf, bare-leaf, triggered-chain, mixed-effects, unknown-chip, and inventory integration. Gap: no test for `apply_chip_effect` when ChipInventory resource is absent (gap (1) from prior tracking, still OPEN).
- `EffectFired` in `events.rs`: `effect_type_matches_shockwave_for_effect_fired` and `effect_type_lose_life_for_effect_fired_with_source_chip` tests call `evaluate_node` which is described in their comments as "This assertion will fail — evaluate_node not implemented". These tests appear to pass (they are in the test suite) yet they include todo!()-guarded paths — the comments say "will fail" but may be green due to how evaluate_node is hooked up. VERIFY that these tests actually fail or are behind a known todo guard.

## Chips domain (apply_chip_effect.rs + effects/, as of 2026-03-19, feature/phase4b2-effect-consumption)
Coverage: very high across happy paths. All 9 effect components tested for insert-on-first-selection. Observer-level stacking + cap tests now present for ALL 9 observers (including the 6 that were previously insert-only: BoltSpeedBoost Amp-chip-component, ChainHit, BoltSizeBoost, BreakerSpeedBoost, BumpForceBoost, TiltControlBoost). Note: BoltSpeedBoost here is the Amp chip component in chips/components.rs — the TriggerChain-level SpeedBoost is tested in behaviors/effects/speed_boost.rs. Gap (6) from previous tracking is CLOSED. PiercingRemaining sync tests added in piercing.rs: first-application, stacking-refreshes, max-stacks-leaves-unchanged, partial-remaining-refreshes. Gap (7) from previous tracking is CLOSED. stack_u32 and stack_f32 now have `_adds_via_world` tests that use a real entity. Gaps (1) and (2) from previous tracking are CLOSED. Known remaining gaps: (1) No test for `apply_chip_effect` when `ChipRegistry` resource is absent. (2) Multi-bolt scenario — Amp chip applied when two Bolt entities exist; both should receive the component (gap 5 from prior tracking). (3) `stack_u32_adds_when_under_cap` and `stack_f32_adds_when_under_cap` inline tests (without real entity) are KEPT alongside the new _via_world tests — they test the local mutation path directly; acceptable as complementary coverage. (4) No test for ignores_non_matching_effect in BoltSpeedBoost, ChainHit, BoltSizeBoost, BreakerSpeedBoost, BumpForceBoost, TiltControlBoost — only piercing.rs and damage_boost.rs have this guard test.

## Wave E — MultiBolt / Shield / MostPowerfulEvolution (as of 2026-03-24, feature/spatial-physics-extraction)
- `multi_bolt.rs`: 6 tests — base-count, stacking formula, self-selection, bolt=None, source_chip None passthrough, stacks=0 saturating. Comprehensive. Known gap: no test for base_count=0 (should spawn zero bolts; edge case untested).
- `shield.rs` observer tests: 5 tests — inserts ShieldActive, stacking formula, additive extension, no-breaker no-panic, self-selection. Comprehensive. Known gap: no test for multiple Breaker entities (single_mut is a guard, but multi-entity behavior is untested).
- `shield.rs` tick tests: 4 tests — decrements, removes at zero-or-below, noop without ShieldActive, removes at exactly zero. Comprehensive.
- `life_lost.rs` shield block tests: 3 tests — blocked by ShieldActive, works without shield, shield protects last life. Comprehensive.
- `time_penalty.rs` shield block tests: 2 tests — blocked by ShieldActive, works without shield. Known gap: no test for shield protecting in both life_lost and time_penalty simultaneously (cross-system coordination); acceptable at this coverage depth.
- `track_evolution_damage.rs`: 5 tests + 1 cross-system test (reset persistence). Comprehensive. No known gaps.
- `detect_most_powerful_evolution.rs`: 7 tests — single evolution, picks highest, tie-breaking, empty no-op, no HighlightTriggered emitted, adds to existing highlights. Known gap: no test for a system called a second time in the same run-end (double detection — system runs OnEnter(RunEnd) so only once per run, but re-entrant call behavior untested).
- `spawn_additional_bolt.rs` SpawnedByEvolution tests: 4 tests — Some inserts, empty string inserts, None omits, multiple attributions. Comprehensive.
- `spawn_chain_bolt.rs` SpawnedByEvolution tests: 2 tests — Some inserts, None omits. Comprehensive.
- `bolt_cell_collision.rs` SpawnedByEvolution attribution tests: 3 tests — attribution carried, none when absent, two bolts different attributions. Comprehensive.
- `spawn_run_end_screen.rs` select_highlights wiring tests: 7 tests — diversity order with config, FIFO fallback without config, cap from config, empty highlights, cap=0, single highlight, MostPowerfulEvolution shows chip name. Comprehensive.

## Wave E — highlight scoring + popups (as of 2026-03-24, feature/spatial-physics-extraction)
- `fx/systems/animate_punch_scale.rs`: 7 tests — midpoint animation, overshoot=1.0 noop, component removed on expire (entity survives), timer crosses zero mid-tick, duration=0.0 no divide-by-zero, entity without Transform unaffected, multiple entities independent. Comprehensive. No known gaps.
- `run/systems/spawn_highlight_text.rs`: 28 tests — all 15 HighlightKind text+color mappings, fade duration from config, cascade stagger, z=10.0, PunchScale from config, cleanup+popup markers, first-popup y=base_y, second popup y stagger, existing popups offset spawn_order, jitter within range, deterministic for same seed, 3 popups distinct positions+stagger, culling to max_visible, no culling when below max, no messages spawns nothing. Comprehensive. Known gaps: (1) no test for `total_after_spawn == max_visible` exactly (boundary: no cull needed but untested). (2) no test for `popup_jitter_min_x == popup_jitter_max_x` (degenerate range).
- `run/systems/select_highlights.rs`: 12 tests — all higher-is-better scored types (7), all lower-is-better scored types (4), binary types (4), edge cases (zero value clamp, at-threshold=0.0, max_expected=1.0 guard, lower-is-better-at-threshold), diversity-penalized selection (different categories top-by-score, same-category penalty, fewer-than-cap, empty input, tie-breaking, no-penalty at 1.0, mixed-category variety). Comprehensive. Known gaps: (1) no test for `diversity_penalty = 0.0` (full category extinction). (2) no test for NaN/Inf in `highlight.value` (lower-is-better path).
- `run/resources.rs` `HighlightKind::category()` method: 15 tests — one per HighlightKind variant. Exhaustive.
- Cap removal: `detect_close_save`, `detect_first_evolution`, `detect_nail_biter`, `detect_combo_and_pinball`, `detect_mass_destruction`, `track_node_cleared_stats` — cap enforcement at detection time removed; selection deferred to run-end `select_highlights`. Tests updated to assert highlights ARE stored beyond old cap of 5.

## Wave 3 — memorable moments detection systems (as of 2026-03-23, feature/wave-3-offerings-transitions)

- `detect_mass_destruction.rs` (4 tests): detected, pruning, message emitted, no-double-record. Known gaps: (1) no test for exactly-at-threshold count (count == mass_destruction_count edge — all tests use count=10 vs threshold=10, so boundary IS implicitly covered). (2) no test for zero messages in a frame (noop path). (3) no test for highlight cap interaction (threshold crossed but cap already full).
- `detect_close_save.rs` (5 tests): detected within threshold, not detected beyond, missing entity, dedup+still-emits, cap enforcement. Comprehensive — all four quadrants covered.
- `detect_nail_biter.rs` (6 tests): detected, not detected, any-bolt-triggers, excludes-BoltServing, no-bolts no-panic, dedup. Comprehensive.
- `detect_first_evolution.rs` (5 tests): detected, not on second, not for non-evolution, graceful without registry, respects cap. Comprehensive. Known gap: no test for multiple ChipSelected messages in same frame where only first is an evolution.
- `detect_combo_and_pinball.rs` (9 tests): counters increment, combo detected/not-detected, best_combo tracking, bounces increment, pinball detected/not-detected, both fire on same hit, dedup. Comprehensive. Known gap: no test for no BoltHitBreaker in frame (counters accumulate but no threshold check).
- `spawn_highlight_text.rs` (18 tests): one per HighlightKind + multi + empty. Exhaustive. No known gaps.
- `track_node_cleared_stats.rs` — `track_node_cleared_stats` does NOT emit `HighlightTriggered` messages (unlike the dedicated detect_* systems). It only writes to `RunStats::highlights`. The detect_* systems emit the message independently. This split is intentional: on-node-clear highlights are batched into RunStats without per-highlight VFX; the detect_* systems handle real-time VFX emission.

## Wave 3 — run/systems stat tracking (as of 2026-03-22)
- `run/resources.rs` (RunStats, HighlightTracker, DifficultyCurve, RunState): unit tests for default values, From conversion, flux_earned (positive + floor-at-zero). Clean. Known gap: no test for flux_earned when all three bonus fields are zero simultaneously (should produce 0, saturating_sub handles it but untested directly).
- `run/systems/track_bolts_lost.rs`: 2 tests — increments stats.bolts_lost, increments tracker.node_bolts_lost. Clean. Known gap: no test for zero messages in a frame (noop path).
- `run/systems/track_bumps.rs`: 5 tests — any grade increments bumps, Perfect increments perfect_bumps, consecutive tracking, non-perfect resets+records streak (>= threshold), non-perfect resets+records streak (< threshold). Clean. Known gap: (1) No test for Late grade specifically vs Early (same branch but different enum variant). (2) No test confirming best_perfect_streak never decreases when current streak < existing best.
- `run/systems/track_cells_destroyed.rs`: 2 tests — multi-message increment, simulation time recorded. Clean. Known gap: no test for zero messages in a frame (noop path).
- `run/systems/track_chips_collected.rs`: 2 tests — single push, accumulate order. Clean.
- `run/systems/track_time_elapsed.rs`: 1 test — 3-tick accumulation. Clean.
- `run/systems/track_node_cleared_stats.rs`: 8 tests — increments nodes_cleared, clutch detected, clutch not-detected, no-damage detected, no-damage not-detected, fast-clear detected, perfect-streak flush+detect, highlight cap at 3. Known gaps: (1) MassDestruction and FirstEvolution HighlightKind variants are defined but no system ever generates them — untested and apparently unimplemented. (2) No test for highlight cap mid-way: e.g., 2 existing highlights and conditions for 2 new ones (only 1 more should be added). (3) No test for no-message frame (noop path).
- `run/systems/reset_highlight_tracker.rs`: 2 tests — resets per-node fields, preserves best_perfect_streak. Known gap: no test that node_start_time is set to current simulated time.
- `run/systems/capture_run_seed.rs`: 3 tests — captures specific seed, only-once guard, generates random when None. Known gap: no test that RNG is re-seeded from the generated seed (rng.0 = ChaCha8Rng::seed_from_u64(stats.seed) path).
- `run/systems/complete_transition_out.rs`: 1 test. System is dead code in Wave 3 — NOT registered in RunPlugin. FxPlugin owns transition animation. Coverage gap is moot (dead system).

## Wave 2a — Two-Phase Destruction + Until + speed_boost/damage_boost (as of 2026-03-25, feature/spatial-physics-extraction)

- `until.rs` `tick_until_timers`: good coverage for decrement, expiry-reversal, non-reversible, empty-removal, independent-removal. Known gap: (1) No test for two entries both expiring in the same tick (both should be reversed and both removed). (2) No test for `ActiveSpeedBoosts`-path reversal vs velocity-fallback path — existing test only exercises the velocity-fallback path (no `ActiveSpeedBoosts` component on bolt). (3) No test for `DamageBoost` removal via `ActiveDamageBoosts` — existing test only exercises the `commands.entity.remove::<DamageBoost>()` fallback.
- `until.rs` `check_until_triggers`: good coverage for Cell, Breaker, Wall, CellDestroyed, cross-trigger negative. Known gap: (1) No test for `OnCellDestroyed` trigger combined with `ActiveDamageBoosts` reversal (only component-remove fallback tested). (2) No test for two bolts: trigger fires for bolt A, bolt B's entry should be unaffected. (3) No test when `UntilTriggers` is present on an entity but no matching message is received in that frame (should be noop, no entry removal).
- `speed_boost.rs`: comprehensive — 17 tests covering scales, clamps, floors, AllBolts, zero-velocity, despawned-bolt, specific-bolt, ActiveSpeedBoosts push + stack, apply_speed_boosts recalculate/clamp/empty/direction/AllBolts. No known gaps.
- `damage_boost.rs`: 7 tests (insert, stack, push-to-vec, stack-in-vec, multiplier-product, empty-vec-returns-1, single-entry). Known gaps (pre-existing): no `respects_max_stacks` test; no `ignores_non_matching_effects` test. These remain OPEN from B12c tracking.
- `bridges.rs` — `bridge_bolt_lost`, `bridge_bump`, `bridge_bump_whiff`, `bridge_cell_impact`, `bridge_breaker_impact`, `bridge_wall_impact`, `bridge_cell_destroyed`, `bridge_cell_death`, `bridge_bolt_death`, `bridge_timer_threshold`, `cleanup_destroyed_cells`, `cleanup_destroyed_bolts`, `apply_once_nodes`, `evaluate_entity_chains`, `resolve_armed`: test suite present (large). Not fully audited here. Known visible gap: `bridge_timer_threshold` non-leaf-child skip path (`_ => {}` arm for non-Do children) has no explicit test. `evaluate_entity_chains` `Arm` result from `Once` children (line 534) is code-path reachable but no test exercises it.
- `handle_cell_hit.rs`: 14 tests. Comprehensive: exact-HP, overkill, partial, zero, locked, non-required, dedup, two-phase RequestCellDestroyed (3 tests), double-on-high-HP, two-different-cells, despawned-entity. No known gaps.
- `bolt_lost.rs`: 14 tests. Comprehensive: position2d detection, respawn-at-breaker-x, zero-spread straight-up, respawn-y, PreviousPosition match, extra-bolt-despawned, extra-bolt-sends-BoltLost, baseline-with-extra-present, RequestBoltDestroyed (2 tests), baseline-no-RequestBoltDestroyed, baseline-sends-BoltLost, above-floor, EntityScale tests (2). No known gaps.
- `cells/messages.rs` + `bolt/messages.rs` + `breaker/messages.rs` test suites: debug-format tests for all new Wave 2a types. These are low-value derive tests but not gaps — the intent is regression guards for message field access, consistent with established codebase pattern.
- `effect/plugin.rs` tests: `plugin_builds`, `effect_plugin_registers_handle_piercing_observer`, `effect_plugin_registers_handle_damage_boost_observer`. Known gap: no wiring test for `handle_speed_boost` (the passive observer path); only triggered observers confirmed.

## Wave 3 — Chip offering, inventory, transition (as of 2026-03-22)
- `chips/offering.rs`: 13 unit tests — empty registry, pool < count, no duplicates, maxed excluded, higher-weight frequency (statistical), decay reduces frequency (statistical), compute_weight arithmetic, same seed, different seed, single chip, all maxed except one, build_active_pool weights+decay, draw_offerings distinct. Known gaps: (1) No test for all chips having zero effective weight (all weights 0.0 after decay) — `WeightedIndex::new` fails in this case and `draw_offerings` breaks early; behavior is untested. (2) `build_active_pool` with a rarity not present in `rarity_weights` falls back to 0.0 — tested implicitly by the weight-test but no explicit named test for the missing-rarity path.
- `chips/inventory.rs`: ~40 unit tests, exhaustive coverage. All methods tested including clear, decay compounding, seen/held independence, maxed_chips iterator. Clean.
- `screen/chip_select/systems/generate_chip_offerings.rs`: 5 integration tests — inserts resource, correct count, records decay, excludes maxed, deterministic with same seed. Known gap: no test for `generate_chip_offerings` when ALL chips are maxed (empty ChipOffers result) — behavior falls through to draw_offerings returning empty but this path is untested at the system level.
- `screen/chip_select/systems/handle_chip_input.rs`: 10 unit tests — right advance, left wrap, confirm transition, confirm message, confirm second card, right wrap-around, no input, empty offers confirm, two-card navigation wrap, confirm records decay for non-selected. Known gap: no test for simultaneous left+right press in the same frame (both guards fire independently — last one wins; behavior is untested).
- `fx/transition.rs`: 9 tests — spawn out/in creates overlay, timer direction out/in, animate ticks timer, out-completion → ChipSelect, in-completion → Playing, cleanup despawns overlay, same seed same style, duration configurable. Known gaps: (1) No test for Flash Out alpha animation mid-progress (the `progress` path in animate_transition for non-zero remaining). (2) No test for Sweep animation (left value update). (3) No test for multiple TransitionTimer entities in one frame (system iterates all — multiple overlays behavior is untested).
- `run/systems/complete_transition_out.rs`: 1 test — sets next state to ChipSelect. The function is declared in `run/systems/mod.rs` as `mod complete_transition_out` but NOT re-exported and NOT registered in `RunPlugin`. It is dead (unreachable) code in Wave 3. The `FxPlugin` now owns transition animation.
- `fx/plugin.rs`: 1 smoke test (plugin_builds). No behavioral tests — acceptable for plugin registration files.
- `run/plugin.rs`: 1 smoke test (plugin_builds). Consistent with existing pattern.
- Scenario runner `check_offering_no_duplicates.rs`: 3 tests — duplicates detected, no duplicates clean, skips when no resource. Known gap: no test for three chips where two share a name (only one duplicate pair tested).
- Scenario runner `check_maxed_chip_never_offered.rs`: 4 tests — maxed chip detected, non-maxed clean, skips no offers, skips no inventory. Clean.

## reset_bolt (bolt/systems/reset_bolt.rs, as of 2026-03-19, feature/phase4b2-effect-consumption)
New system with 9 tests. Coverage: reposition above breaker, zero velocity on node 0, initial velocity on subsequent nodes, inserts BoltServing on node 0, removes BoltServing on subsequent nodes, resets PiercingRemaining to Piercing, preserves chip effect components untouched, snaps Position2D (was PhysicsTranslation pre-2026-03-24 extraction), noop when no bolt. Known gaps: (1) No test for reset_bolt when breaker entity is absent — system falls back to (0.0, 0.0) but this is untested. (2) No test for x-centering above a horizontally offset breaker — reset_bolt uses breaker_x but the existing test uses breaker_x=0.0. (3) No test confirming reset_bolt is a noop on ExtraBolt entities (test_bolt_ignores_extra_bolt_entities checks the extra bolt is NOT repositioned, but doesn't check velocity or other fields on it). Gap (3) is low-priority.

## bridge_bump_whiff (behaviors/bridges.rs, as of 2026-03-19, feature/phase4b2-effect-consumption)
New bridge with 4 tests: fires LoseLife, no-message no-consequence, wrong-trigger isolation, triggers TimePenalty. Clean — negative case (wrong trigger) explicitly covered. Known gap: no test for multiple BumpWhiffed messages in the same frame — bridge drains all, but only fires consequences once (same pattern as bridge_bolt_lost). This behavior is untested but the drain-all semantics are documented in the function doc.

## Seed entry / run setup (as of 2026-03-19)
- `handle_seed_input`: excellent coverage — tab toggle, focused/unfocused digit filtering, non-digit rejection, backspace, escape, max-char cap, release-event filtering. Known gap: no test for Backspace when `value` is already empty (pop on empty String is a no-op but verifying it doesn't crash is defensive). No test for Tab→Escape interaction (focus off via Escape after Tab on).
- `update_seed_display`: all four state combos tested (empty/unfocused, value/unfocused, empty/focused, value/focused). Color tested for focused and unfocused. Clean.
- `handle_run_setup_input` seed integration: `confirm_sets_run_seed_none_when_entry_empty`, `confirm_sets_run_seed_some_when_entry_has_value`, `confirm_sets_run_seed_zero_on_parse_failure` — parse-failure fallback produces `Some(0)` (not `None`). No test verifies behavior when `SeedEntry` resource is absent (the `Option<Res<SeedEntry>>` guard).

## Wave 1 — spatial2d new systems (compute_globals, derive_transform, apply_velocity, save_previous, plugin, as of 2026-03-23)
- `compute_globals.rs`: 9 tests — root entity copies for position/rotation/scale (3), relative child combines for all three (3), absolute child ignores parent for all three (3). Known gaps: (1) No test for child whose parent is NOT in the parent cache (the fallback-to-local branch at line 70-75) — this path fires when a child's parent has no Spatial2D components. (2) No test for a 3-level hierarchy (grandparent → parent → child); only single parent/child tested. (3) No test for an entity without any propagation component (`None` arm of `Option<&PositionPropagation>` in child pass) — defaults to Relative treatment, untested directly.
- `derive_transform.rs`: 7 tests — non-interpolated from globals (all 3 fields), interpolated at alpha ~0.5 (position only), interpolated at alpha ~0 (position only), visual offset added, visual offset z stacks with DrawLayer z, entity without DrawLayer not processed. Known gaps: (1) No test for interpolated rotation at mid-alpha (only position is tested at ~0.5). (2) No test for interpolated scale at mid-alpha. (3) No test for entity with InterpolateTransform2D but WITHOUT Previous* components (falls back to current Global* value — the `else` branches inside the interpolation guard). (4) No test asserting rotation defaults to identity when GlobalRotation2D is identity (the non-interpolated path for rotation is exercised in `non_interpolated_transform_from_globals` but the expected value is 45 degrees, not the identity case).
- `apply_velocity.rs`: 4 tests — applies velocity to position, no effect without Velocity2D, handles negative velocity, no effect without ApplyVelocity marker. Known gap: no test for zero velocity (pos unchanged — trivially implied but not explicitly named).
- `save_previous.rs`: 13 tests — copies position/rotation/scale to previous (from local and from global), skips without interpolate marker (for position, rotation, scale), multiple entities updated, snapshots global variants over local, velocity snapshot and its absence/marker-less guards. Comprehensive. Known gaps: (1) No test for entity with both GlobalPosition2D and GlobalRotation2D absent (only `None` for one at a time is tested; the fallback path in `map_or` is only exercised partially). (2) No test for `save_previous` when the entity has both Velocity2D and PreviousVelocity but is missing Position2D/PreviousPosition — the velocity sub-query is independent but its isolation is not directly tested.
- `plugin.rs`: 5 behavioral tests + 1 build test — builds, computes globals for root entity, derives transform from globals, applies velocity with marker, skips velocity without marker, and registers new type reflections. Known gap: no test for the scheduling order (save_previous → apply_velocity → propagate_* / compute_globals / derive_transform chain); ordering is verified by wiring, not by test assertion.

## PreviousScale / propagate_scale (as of 2026-03-23, feature/wave-3-offerings-transitions)
- `PreviousScale` component: default test, copies_scale_to_previous_scale, skips_scale_without_interpolate_marker. Clean. Known gap: no test for PreviousScale on an entity with `Spatial2D` that goes through a full fixed-update pipeline (integration level).
- `propagate_scale`: basic, entity_without_scale, relative/absolute parent/child, interpolated half-alpha, alpha-zero, high-alpha, absolute+interpolation. Known gap: no test for division-by-zero in absolute mode when parent_scale.x or parent_scale.y is 0.0 (Scale2D::new prevents this at construction but parent entity could have stale scale after forced mutation).
- `#[require]` tests for Bolt/Breaker/Cell/Wall: all cover positive (required components present) and negative (cleanup NOT auto-inserted). Pattern is consistent and comprehensive.

## Lifecycle Tests (2026-03-17, updated 2026-03-18)
Cover all major public systems: tick_scenario_frame, check_frame_limit, apply_debug_setup, enforce_frozen_positions, tag_game_entities, inject_scenario_input, init_scenario_input, ScenarioStats increments.
`restart_run_on_end` gap was closed — `restart_run_on_end_transitions_to_main_menu` test exists at `breaker-scenario-runner/src/lifecycle/tests.rs:808`. No longer a gap.

## Overclock evaluation engine (behaviors/, as of 2026-03-21, refactor/unify-behaviors merged — was bolt/behaviors/ before unification)
Coverage: good across the core evaluation and bridge systems. Unit tests cover all 10 TriggerKind variants in behaviors/evaluate.rs, all bridge functions in behaviors/bridges.rs (including two-step surge chain and bolt-specific arming), handle_overclock in chips/effects/overclock.rs. shockwave.rs (now behaviors/effects/shockwave.rs) has 10 unit tests + 1 E2E test (full surge pipeline). lifecycle/mod.rs has 2 new tests for ActiveChains population from initial_overclocks. New bridges: bridge_breaker_impact, bridge_wall_impact, bridge_bump, bridge_bump_whiff. New TriggerChain variants fully wired: OnEarlyBump, OnLateBump, OnBumpWhiff (triggers in TriggerKind); LoseLife, SpawnBolt, TimePenalty, SpeedBoost (was BoltSpeedBoost — leaves with registered handlers). handle_speed_boost in behaviors/effects/speed_boost.rs handles TriggerChain::SpeedBoost. Known gaps:
(1) `bridge_cell_destroyed` — no test that evaluates ARMED triggers on all bolts when a cell is destroyed (the `evaluate_armed_all` path from CellDestroyed is not exercised).
(2) `bridge_bolt_lost` — same gap: no test for armed triggers on all bolts when bolt is lost.
(3) `handle_overclock` — no test for `ChipEffect::Augment` being ignored (only `Amp` is tested as a negative case).
(4) `ActiveChains` (was `ActiveOverclocks`) with multiple chains — no test asserting all chains are evaluated on a single trigger (only single-chain scenarios tested in bridges.rs).
(5) `evaluate_active_chains` — no test exercising the `Arm` result path when CellDestroyed/BoltLost fires (active chain arming is silently discarded in these bridges; no test confirms this is intentional).
(6) `shockwave.rs` (now behaviors/effects/shockwave.rs) — no test for distance exactly equal to range boundary (dist == range should hit, dist > range should not); existing tests use distance well inside or outside range.
(7) `bypass_menu_to_playing` — no test for when `ActiveChains` (was `ActiveOverclocks`) resource is absent but `initial_overclocks` is `Some` (the `Option<ResMut<ActiveChains>>` guard silently skips population; this path is untested).
(8) `source_bolt` field on `DamageCell` is always `Entity::PLACEHOLDER` in ALL tests across handle_cell_hit.rs and check_lock_release.rs — the VFX-attachment semantic of this field is untested.
(9) `bridge_breaker_impact` — no test for BreakerImpact chain NOT matching OnImpact(Cell, ...) or OnImpact(Wall, ...) (negative ImpactTarget discrimination not tested for breaker bridge; only covered in evaluate.rs unit tests).
(10) `bridge_wall_impact` — same gap: wall bridge has no negative-target discrimination test at the bridge level.
(11) RESOLVED (refactor/unify-behaviors merged 2026-03-21): OnEarlyBump, OnLateBump, OnBumpWhiff are wired — TriggerKind in behaviors/evaluate.rs has EarlyBump, LateBump, BumpWhiff variants; evaluate() handles all 10 trigger kinds; bridge_bump maps grades accordingly.
(12) RESOLVED (refactor/unify-behaviors merged 2026-03-21): LoseLife, SpawnBolt, TimePenalty, SpeedBoost (was BoltSpeedBoost) leaf variants all have registered observers: handle_life_lost, handle_time_penalty, handle_spawn_bolt, handle_speed_boost in behaviors/effects/.
(13) RESOLVED (refactor/unify-behaviors merged 2026-03-21): bridge_bump_whiff exists in behaviors/bridges.rs; bridge_bump routes EarlyBump and LateBump correctly via TriggerKind.

## EntityScale feature (as of 2026-03-20, feature/overclock-trigger-chain branch)
Systems: apply_entity_scale_to_breaker, apply_entity_scale_to_bolt, bolt_scale_visual, width_boost_visual, bolt_breaker_collision, bolt_cell_collision, clamp_bolt_to_playfield, bolt_lost, spawn_additional_bolt, definition.rs validate.
Coverage: very high. All systems have backward-compat (no EntityScale) + scaled + EntityScale(1.0) identity tests. Known gaps:
(1) `apply_entity_scale_to_breaker`: no test for multiple Breaker entities in one frame — system loops the query but only one breaker is ever spawned in tests.
(2) `bolt_lost`: no test for a scaled bolt that is ABOVE the effective lost threshold (should not be detected as lost) — only the positive (below threshold, lost) case is tested.
(3) `bolt_breaker_collision`: no test for scaled bolt + scaled breaker simultaneously — existing tests scale only one side.
(4) `clamp_bolt_to_playfield`: left-wall clamp with EntityScale has no vy-unchanged assertion (right-wall scaled test also lacks it — the plain left-wall test does assert vy unchanged, but the scaled version only checks x position).

## Documentation Conventions
- Module-level `//!` doc comments on all `.rs` files.
- Public types/functions have `///` doc comments with field-level docs.
- Private helpers often lack doc comments (accepted when name is self-describing).
- Units documented inline on component fields (e.g., "world units per second").
- `#[must_use]` applied to pure query methods and value-returning helpers.
