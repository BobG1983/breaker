# reviewer-quality Memory

## Codebase Patterns
- [Drop-to-discard pattern](pattern_drop_discard.md) — intentional use of `drop(result)` to silence `#[must_use]` warnings in scenario runner tooling code
- [Scenario runner vocabulary](pattern_scenario_runner_vocab.md) — game vocabulary compliance is not enforced for `breaker-scenario-runner` tooling code
