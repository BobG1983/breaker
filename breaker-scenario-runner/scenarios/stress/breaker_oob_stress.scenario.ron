(
    breaker: "Aegis",
    layout: "Corridor",
    input: Chaos((
        seed: 42,
        action_prob: 0.3,
    )),
    max_frames: 5000,
    invariants: [BoltInBounds, BreakerInBounds, BreakerPositionClamped, NoNaN, ValidStateTransitions],
    expected_violations: None,
    debug_setup: None,
    allow_early_end: false,
    stress: Some(()),
)
