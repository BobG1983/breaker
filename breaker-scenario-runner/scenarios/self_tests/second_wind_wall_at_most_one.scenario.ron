(
    breaker: "Aegis",
    layout: "Corridor",
    input: Scripted((
        actions: [],
    )),
    max_frames: 120,
    disallowed_failures: [BoltInBounds, BreakerCountReasonable, BreakerInBounds, NoNaN, SecondWindWallAtMostOne],
    allowed_failures: Some([SecondWindWallAtMostOne]),
    debug_setup: (
        disable_physics: true,
    ),
    // Spawn 2 extra SecondWindWall entities at frame 30 — count becomes ≥ 2
    // (at least 2 bare markers, likely more if effect spawned one already).
    // The invariant fires as soon as count > 1.
    frame_mutations: [
        (frame: 30, mutation: SpawnExtraSecondWindWalls(2)),
    ],
)
